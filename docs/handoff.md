# Handoff — State-Method Test Coverage

> **Live state only.** The contract — principles, vocabulary table, roadmap — lives in [unique-agent-strategy.md](unique-agent-strategy.md). This file records the current shape of the system and what to pick up next.
>
> Update at the end of every session that advances the roadmap.

## Current state

The capability vocabulary is 7 verbs, all enforced through one shared authorizer:

| Verb | Manifest declaration | Runtime enforcement |
|---|---|---|
| `tool:<name>` / `tool:*` | yes | `CapabilityAuthorizer` from `src/Tools/Weave.Tools/Actors/ToolActor.cs` |
| `secret:<path>` | yes | `CapabilityAuthorizer` from `src/Security/Weave.Security/Vault/VaultSecretProvider.cs` and `InMemorySecretProvider.cs` |
| `skill:read` / `skill:write` | yes | `CapabilityAuthorizer` from `src/Assistants/Weave.Agents/Actors/SkillMemoryActor.cs` |
| `channel:send:<id>` / `channel:receive:<id>` | yes | `CapabilityAuthorizer` from `src/Assistants/Weave.Agents/Actors/ChannelGatewayActor.cs` |
| `user:read:<userId>` / `user:write:<userId>` | yes | `CapabilityAuthorizer` from `src/Assistants/Weave.Agents/Actors/UserModelActor.cs` |
| `plugin:invoke:<plugin>` | yes | `CapabilityAuthorizer` from `src/Runtime/Weave.Silo/Plugins/PluginRegistry.cs` |
| `marketplace:install` | n/a (silo-wide; minted at API boundary) | `CapabilityAuthorizer` from `src/Tools/Weave.Tools/Actors/MarketplaceActor.cs` |

Tests: 2239 total — 2230 passed, 9 skipped when Docker is unavailable. The 9 cover `PostgresCapabilityAuditStore` against a Testcontainers-managed Postgres; the fixture probes Docker by attempting to start the container and self-skips each test with the captured reason on Docker-less hosts. (1852 → 1855 → 1861 → 1866 → 1879 → 1899 → 1895 → 1912 → 1942 → 1958 → 1976 → 1991 → 1997 → 2000 → 2016 → 2027 → 2048 → 2063 → 2108 → 2113 → 2137 → 2170 → 2175 → 2206 → 2226: latest +20 cover the security-review-driven hardening of `IsSafeWorkspaceName` (4 new path-traversal rows for Windows drive-relative shapes `C:foo`/`C:`/`D:..\\evil`/`foo:bar`; new `_WindowsReservedNames_AreRejected` Theory with 12 rows for `CON`/`PRN`/`AUX`/`NUL`/`COM1`/`COM9`/`LPT1`/`LPT9` plus case-insensitive + extension forms `con`/`Nul`/`CON.txt`/`nul.tar.gz`) plus 4 `SanitizeForEcho` Facts (printable-passthrough, ANSI-ESC-replaced, NL+TAB+BEL replaced, DEL replaced). Earlier +31 cover the recent UX hardening: `MarketplaceInstallCliCommandTests` (18 — `IsSafeWorkspaceName` theory with 7 safe + 11 path-traversal rows, install-command option-presence + parse + defaults assertions), `WorkspaceManifestPathsTests` (10 — was untested despite being used in 9+ places: `GetStatePath` absolute/relative resolution, `PrepareForSilo` relative→absolute rewrite + absolute-passthrough + blank-passthrough theory). Earlier +5 cover the Down UX-conflation drain (3) and the blank-`--workspace-id` source-attribution fix (2 `[Theory]` rows). Earlier +33 cover Phase 4c — `BrowseMarketplaceItemsActionTests` (4), `SearchMarketplaceActionTests` (5 incl. `[Theory]` blank-query rows), `GetMarketplaceItemActionTests` (4), `SubmitMarketplaceItemActionTests` (4), `PublishMarketplaceItemActionTests` (4), `InstallMarketplaceItemActionTests` (5), `GetCapabilityAuditByTokenActionTests` (4), `GetRecentCapabilityAuditActionTests` (3) — covering the typed marketplace verbs and the audit replay queries that drained `MarketplaceApiClient` and `AuditApiClient`. Earlier +24 cover Phase 4b — `ListSkillsActionTests` (5), `PostSkillActionTests` (5: success/badrequest/unauthorized/unreachable/endpoint), `ListChannelsActionTests` (4), `PostChannelActionTests` (4), `ListTemplatesActionTests` (3), `ListMarketplaceItemsActionTests` (3) — covering the new opaque-JsonElement data-roundtrip verbs. Earlier +5 cover Phase 3.5 — `ManifestResolverTests` (3: registered-name with manifest file present, registered-name with manifest missing, unknown-name) and `WorkspaceCompletionsTests` (2: registry pass-through, empty registry). Earlier +45 cover `SetConfigAction` (~14: case-insensitive key match `[Theory]`, unknown-key validation with key-list hint, defaultPort numeric `[Theory]` with port range checks, blank siloPath, writer-throws-IOException → `Internal`, writer-throws-Unauthorized → `Unauthorized`, null/blank input guards), `OpenWorkspaceAction` (~9: registered-name match, case-insensitive `[Theory]`, locator-resolves-from-CWD-fallback, no-name-empty-registry → `NotFound` with create hint, no-name-with-registry → prompt + selection, prompt-cancel → `Cancelled`, locator-null → `NotFound`, null-input), `SelectAgentAction` (~9: requested name match returns canonical casing, missing → `NotFound`, single-candidate auto-select, multi-candidate prompt path, prompt-cancel, empty-list, null/blank guards), and `WatchWorkspaceAction` (~12: composite-success, status-fail propagates verbatim, agent-or-tool-fail → empty list + still-success best-effort, status-unreachable, null/blank guards).

### How a verb is wired today

Six sites that previously held a private `Authorize(...)` method (or, in the case of the secret providers, an inline `Validate` + `HasGrant` pair) now share `ICapabilityAuthorizer` ([CapabilityAuthorizer.cs](../src/Security/Weave.Security/Tokens/CapabilityAuthorizer.cs)). The authorizer:

1. **Validates** signature, expiry, revocation via `ICapabilityTokenService.Validate`. Validate accepts signatures under either `CapabilityTokens:SigningKey` or — when configured — the verify-only `CapabilityTokens:PreviousSigningKey`, so a rotation can swap keys without invalidating in-flight tokens.
2. **Workspace-matches** when `actorWorkspaceId` is non-empty (PluginRegistry passes `null` since plugins are silo-wide).
3. **Grant-checks** via `CapabilityToken.HasGrant` (segment-wise wildcards).
4. **Publishes** `CapabilityAuthorizationEvent` ([CapabilityAuthorizationEvent.cs](../src/Security/Weave.Security/Events/CapabilityAuthorizationEvent.cs)) on every call (allow OR deny) keyed by `tokenId`, `grant`, `workspaceId`, `issuedTo`, `outcome`, `actionContext`, and `reason` on denies (`"invalid-or-expired-token"` / `"workspace-mismatch"` / `"grant-missing"`).
5. **Throws** `UnauthorizedAccessException` on deny.
6. **Increments** a `System.Diagnostics.Metrics.Counter<long>` (`weave.security.capability.authorizations`) on every call, tagged with `outcome` (`"allow"`/`"deny"`) and, on deny, `reason`. Meter name `Weave.Security.Capability` is registered in `Weave.ServiceDefaults.Extensions.ConfigureOpenTelemetry` so the existing OTLP pipeline exports it without any per-host wiring.

Call sites pass `actionContext` as a literal string (e.g. `"ChannelGatewayActor.RouteInbound:receive"`) or default to `[CallerMemberName]`.

DI registration lives in `src/Runtime/Weave.Silo/Startup/SiloServiceRegistrar.cs` `RegisterSecurity()`. Per-request token minting at the API boundary, manifest-gated runtime mints, and cancellation propagation are unchanged.

### How replay/debugger is wired today

Roadmap #3 ships as a thin pipeline on top of the audit event. No producer-side changes:

1. **Store** — singleton, capacity-bounded, FIFO eviction at `CapabilityAudit:Capacity` (default 10,000). Three backends, selected by `CapabilityAudit:Backend`. **Each backend lives in its own project so `Weave.Security` itself stays free of storage-provider dependencies** (the in-memory store ships there because it has no third-party dep):
   - `"memory"` (default) — [`InMemoryCapabilityAuditStore.cs`](../src/Security/Weave.Security/Audit/InMemoryCapabilityAuditStore.cs); LinkedList behind a `Lock`. Evicts on silo restart.
   - `"sqlite"` — [`SqliteCapabilityAuditStore.cs`](../src/Security/Weave.Security.Sqlite/Audit/SqliteCapabilityAuditStore.cs) in `Weave.Security.Sqlite` (drags `Microsoft.Data.Sqlite`); single-connection SQLite at `CapabilityAudit:ConnectionString` (default `~/.weave/audit.db`). Single-silo durability.
   - `"postgresql"` — [`PostgresCapabilityAuditStore.cs`](../src/Security/Weave.Security.Postgres/Audit/PostgresCapabilityAuditStore.cs) in `Weave.Security.Postgres` (drags `Npgsql`); pooled `NpgsqlConnection` at the required `CapabilityAudit:ConnectionString`. Multi-silo durability — every silo writes into one shared `capability_audit` table.
   All three implement `ICapabilityAuditStore` (defined in `Weave.Security`); the selector lives in `SiloServiceRegistrar.RegisterCapabilityAuditStore()`. The Silo references both `Weave.Security.Sqlite` and `Weave.Security.Postgres` directly — they are wired by configuration, not discovered. SQL backends auto-apply the schema on construction and use trim-on-insert to keep the same FIFO bound. `GetByToken(tokenId)` filters chronologically; `GetRecent(limit)` walks newest-first.
2. **Subscriber** ([`CapabilityAuditSubscriberHostedService.cs`](../src/Runtime/Weave.Silo/Audit/CapabilityAuditSubscriberHostedService.cs)) — `IHostedService` that subscribes to `IEventBus` for `CapabilityAuthorizationEvent` in `StartAsync` and disposes in `StopAsync`. Each row is handed to `store.Record` inside a bounded retry loop (`MaxAttempts = 3`, exponential backoff with full jitter, `BaseDelay = 50ms`, `MaxDelay = 500ms`, `Task.Delay` on the injected `TimeProvider`, retries cancelled at shutdown). A static `Counter<long>` named `weave.silo.audit.write_failures` (meter `Weave.Silo.Audit`, registered in `Weave.ServiceDefaults.Extensions.ConfigureOpenTelemetry`) increments tagged `outcome="retried"` when an attempt succeeds after a prior failure and `outcome="dropped"` when every attempt fails. Exceptions are swallowed at the handler boundary (the in-process bus already snapshots and isolates handler failures); the authorize-side `await PublishAsync` therefore blocks at most one full backoff window in the worst case before the row is dropped.
3. **Queries** — `GetCapabilityAuditByTokenQuery(tokenId)` and `GetRecentCapabilityAuditQuery(limit)` in `src/Security/Weave.Security/Queries/`. Picked up by source-generated CQRS registration like every other query handler.
4. **HTTP** — `GET /api/audit/capability/{tokenId}` and `GET /api/audit/capability?limit=N` ([`AuditEndpoints.cs`](../src/Runtime/Weave.Silo/Api/AuditEndpoints.cs)). Returns `CapabilityAuditEntryResponse[]` (`outcome` as string for stability).
5. **CLI** — `weave audit replay [tokenId]` ([`AuditReplayCliCommand.cs`](../src/UX/Weave.Cli/Commands/Audit/AuditReplayCliCommand.cs)). Guided mode: zero-arg invocation lists distinct recent tokens via Spectre `SelectionPrompt`; advanced mode: pass the tokenId. Renders allow/deny trace as a Spectre table coloured by outcome.
6. **Dashboard** — `/audit` and `/audit/{tokenId}` ([`Audit.razor`](../src/UX/Weave.Dashboard/Pages/Audit.razor)). Defaults to recent rows; deep links to per-token replay; token cells link back to the per-token view. Uses `WeaveApiClient.GetRecentCapabilityAuditAsync` / `GetCapabilityAuditByTokenAsync` so any future backend swap (durable store) is invisible to the page.

Acceptance bar from the strategy doc's *Measurement* section is met:
- **Audit completeness** — every Authorize call (allow + deny) publishes a row carrying capability, grant, actor, reason. Verified by the per-actor smoke tests added in roadmap #2 + the end-to-end `CapabilityAuditEndpointTests`.
- **Coverage of action types** — 6/6 actor sites publish through the shared authorizer.

### How capability templates are wired today

The strategy doc named capability templates as the promoted roadmap item ("they already are pre-validated capability bundles. Lean in."). This session cashed that claim in two halves:

1. **Preset hydration** — every workspace preset in `src/UX/Weave.Cli/Commands/Workspace/WorkspacePresets.cs` now declares a `Capabilities` array that covers the tools (and channels/skill memory/user model, where the preset wires them). `WorkspaceNewSelection` carries the list through the prompt; `WorkspaceNewTemplateFactory.{CreateSingleAgent,CreateMultiAgent,CreateSupportTeam}` assigns it to the emitted `AgentDefinition.Capabilities`. The custom flow (no preset) derives a baseline `tool:<name>` grant per chosen tool so the manifest is self-coherent on creation. Today's preset → grant mapping:
   - `starter` → `[]`
   - `coding-assistant` → `["tool:git", "tool:files"]`
   - `research` → `["tool:web-search", "tool:files"]`
   - `multi-agent` → `["tool:git", "tool:files", "tool:web-search"]` (both supervisor + worker)
   - `support-team` → support-bot gets `["tool:web-search", "tool:files", "channel:send:slack", "channel:receive:slack", "skill:read", "skill:write", "user:read:*", "user:write:*"]`; the hard-coded monitor agent gets `["tool:web-search"]`

2. **Validator coherence** — `CapabilityTemplateActor.Validate` ([CapabilityTemplateActor.cs:110](../src/Workspaces/Weave.Workspaces/Actors/CapabilityTemplateActor.cs)) now emits a `ToolCapabilityGranted:<tool>` check for every tool the agent references and refuses to publish a template where the grant is missing. The same loop walks `RequiredCapabilities` and emits a `RequiredCapabilityGranted:<grant>` check. Both checks coalesce a null `AgentDefinition.Capabilities` to `[]` (STJ/Orleans round-trip can drop the init-only initializer), so the validator fails closed instead of throwing. A wildcard like `tool:*` covers every tool — same segment-wise rule the runtime uses.

The matcher itself moved down: `CapabilityGrantMatcher.HasGrant(IEnumerable<string>, string)` now lives in `Weave.Shared/Capabilities/` so both `Weave.Workspaces` (validator) and `Weave.Agents` (manifest gates) call it without `Weave.Workspaces` having to depend on `Weave.Security`. The instance method `CapabilityToken.HasGrant(string)` is a one-liner delegate to the matcher; the static `CapabilityToken.HasGrant(IEnumerable<string>, string)` and its private `Matches` helper are gone (replaced wholesale, no shim — the four `Weave.Agents` call sites and the six `Weave.Security.Tests` cases moved to the new home).

### How curated templates ship today

Five pre-validated templates (`tpl-built-in-{starter,coding-assistant,research,multi-agent-supervisor,support-bot}`) live in [`BuiltInTemplates`](../src/Workspaces/Weave.Workspaces/Templates/BuiltInTemplates.cs) in `Weave.Workspaces`. Each is exposed as a named `CapabilityTemplate` field (`BuiltInTemplates.Starter`, `.CodingAssistant`, `.Research`, `.MultiAgentSupervisor`, `.SupportBot`) and aggregated into `BuiltInTemplates.All`. Author `"weave"`, version `"1.0.0"`, tagged `["built-in", "weave"]`. One entry per primary agent shape — multi-agent workers and the support-team monitor stay in CLI preset definitions because they're composition concerns, not template concerns.

Seeding is a hosted service, [`BuiltInTemplateSeeder`](../src/Runtime/Weave.Silo/Templates/BuiltInTemplateSeeder.cs), registered next to `CapabilityAuditSubscriberHostedService` in `SiloServiceRegistrar.RegisterAgentPipeline()`. On `StartAsync` it walks `BuiltInTemplates.All`, queries each `TemplateId` via `ICapabilityTemplateActor.GetAsync("global")`, and for any missing entry calls `RegisterAsync` + `ValidateAndPublishAsync`. Idempotent — a second silo start (or a persistent template backend across restarts) skips every entry. Validation failures log at `Error` and continue past the offending template rather than faulting silo startup.

The CLI's [`PresetDefinition`](../src/UX/Weave.Cli/Commands/Workspace/PresetDefinition.cs) wraps a `CapabilityTemplate PrimaryTemplate` and exposes `Model`, `Tools`, `ToolDefinitions`, `Capabilities` as computed properties that delegate to the template. `WorkspacePresets.All` constructs each entry by passing `BuiltInTemplates.X` directly (`PrimaryTemplate: BuiltInTemplates.CodingAssistant` etc.), so the agent-shape data is one object referenced from two surfaces — not two coordinated copies. The five-row drift-detector theory is gone, replaced by a one-line `ShouldBeSameAs` reference-equality check per preset.

### How `marketplace:install` is wired today

`MarketplaceItem` carries an optional `TemplateId? TemplateId` that links the item to a published template ([MarketplaceItem.cs](../src/Tools/Weave.Tools/Marketplace/MarketplaceItem.cs)). `IMarketplaceActor.InstallAsync(itemId, token)` ([MarketplaceActor.cs](../src/Tools/Weave.Tools/Actors/MarketplaceActor.cs)) is the install action: authorizes `marketplace:install` (workspace-scope `null` since marketplace is silo-wide, like `plugin:invoke`); refuses non-Published items (409) and items without a linked template (409); resolves the linked `CapabilityTemplate` via `ICapabilityTemplateActor.GetAsync` (404 if the template was deprecated); increments both `MarketplaceItem.InstallCount` and `CapabilityTemplate.InstantiationCount`; returns a `MarketplaceInstallResult` carrying the resolved template.

Token mint at the API boundary: [`MarketplaceTokenFactory.MintInstall`](../src/Runtime/Weave.Silo/Api/MarketplaceTokenFactory.cs) is the silo-wide equivalent of `PluginTokenFactory.MintInvoke` — 1-minute lifetime, single-grant `marketplace:install`, `WorkspaceId="silo"`. The HTTP endpoint `POST /api/marketplace/{itemId}/install` ([MarketplaceEndpoints.cs](../src/Runtime/Weave.Silo/Api/MarketplaceEndpoints.cs)) mints, calls the actor, and returns `MarketplaceInstallResponse` (item + template).

CLI: `weave marketplace install [item-id]` ([MarketplaceInstallCliCommand.cs](../src/UX/Weave.Cli/Commands/Marketplace/MarketplaceInstallCliCommand.cs)). Guided mode picks from a Spectre selector via `MarketplaceItemPrompt`; advanced mode passes the id. After the API records the install, the CLI prompts for a workspace name (default = template name) and materializes a workspace under that path: creates `prompts/`, `data/`, `.weave/`, registers in `WorkspaceRegistry`, writes `workspace.json` from the template via the shared primitive, and writes a default system-prompt file derived from the template description. Refuses to scaffold over a non-empty directory (no overwrite).

### How the marketplace → workspace primitive is wired today

`WorkspaceManifestFromTemplate.Create(template, workspaceName, isolation, agentName = "assistant")` in `src/Workspaces/Weave.Workspaces/Templates/WorkspaceManifestFromTemplate.cs` is the one composition point: given a `CapabilityTemplate`, a workspace name, and an isolation level, it returns a `WorkspaceManifest` carrying one agent (keyed by `agentName`) seeded from `template.AgentDefinition` with `SystemPromptFile` rewritten to `./prompts/{agentName}.md`, plus `template.RequiredTools` copied into `manifest.Tools`, a `weave-{workspaceName}` network, `env`-secrets, and a default `local`/`podman` target. Pure — no IO, no clock, no DI.

Two consumers share it:
- **`MarketplaceInstallCliCommand.ScaffoldWorkspaceAsync`** rebuilds a `CapabilityTemplate` from the install API response, calls the primitive with `agentName = "assistant"`, then writes the manifest + a `# {Name}\n\n{Description}\n` prompt file.
- **`WorkspaceNewTemplateFactory`** uses the primitive as the base for every preset path: single-agent presets (starter / coding-assistant / research) bottom out at the primitive's output; `multi-agent` calls it with `agentName = "supervisor"` and adds a hard-coded `worker`; `support-team` calls it with `agentName = "support-bot"` and adds the hard-coded `monitor`. The `custom` (no-preset) path bypasses the primitive — there's no template to compose from.

Touches needed to keep the API DTO honest: `TemplateResponse` (silo) and `ApiMarketplaceInstallTemplate` (CLI) both gained `AgentDefinition AgentDefinition` + `Dictionary<string, ToolDefinition> RequiredTools`. Source-gen JSON contexts pull the nested `Mcp/Cli/OpenApi/DirectHttp/FileSystem` configs in transitively.

### Wave 5 vertical slices: drained

The three domain projects that previously held horizontal `Actors/`, `Commands/`, `Queries/`, `Events/` folders now organize by capability:

- **`Weave.Workspaces`**: `Lifecycle/`, `Registry/`, `Templates/`, `Manifest/` — the `Actors/Commands/Queries/Events/` and `Models/` folders are gone.
- **`Weave.Tools`**: `Tool/`, `Marketplace/`, `Connectors/` — the `Actors/Events/` and `Models/` folders are gone.
- **`Weave.Agents`**: `Lifecycle/`, `ToolRegistry/`, `Channels/`, `Memory/`, `Skills/`, `Users/`, `Verification/`, `Chat/` — the `Actors/Commands/Queries/Events/` and `Models/` folders are gone.

Both behaviour-namespaces (the actor/command/query/event ones) and data-namespaces (state, status, info, config) now match their folder. Files that previously declared `Weave.X.Models` (97 of them across the three domains) now declare `Weave.X.<Folder>` matching where they live — `Weave.Workspaces.Manifest` for the manifest-shape records, `Weave.Tools.Tool` for tool definitions/specs, `Weave.Agents.Memory` for episode records, etc. Cross-folder references inside the previously-flat `Models` namespace now use explicit `using Weave.X.<Folder>;` directives.

### How Shape C is wired today

The action-layer foundation lives in [`src/UX/Weave.Actions`](../src/UX/Weave.Actions/). Eight verbs ship and **every Phase 1 TUI consumer is migrated**: `weave system` (Phase 0 pilot), and Phase 1 reads `weave agents`, `weave tools`, `weave tasks`, `weave workspace status`, `weave workspace validate`, `weave config get`, `weave webui` (each with a migrated TUI slash where applicable). Most actions **go through the silo** — including `ValidateWorkspaceAction`, which reads the manifest from disk on the frontend and POSTs the JSON(C) text to `POST /api/workspaces/validate` so the silo's `ManifestParser` + structural-validation logic is the single source of truth. Two verbs are local-only by nature: `GetConfigAction` reads `~/.weave/config.json` (frontend-shaped data the silo doesn't know about), and `GetDashboardStatusAction` probes the dashboard URL (a different process from the silo). Both still own their I/O end-to-end; both **do not depend on any "silo client" abstraction** — actions that talk to the silo each take a typed `HttpClient` and own the call, actions that don't talk to the silo take only `ISystemConfigSource`.

1. **Context primitives** ([Context/](../src/UX/Weave.Actions/Context/)). `IActionPrompter` (`PromptText`/`PromptSelection`/`PromptConfirm`), `IActionReporter` (`Info`/`Success`/`Warning`/`Error`), `ActionResult<T>` (typed success/failure record struct with a non-generic `ActionResult` factory companion to satisfy CA1000), `ActionFailure` (record carrying an `ActionFailureReason` enum: `SiloUnreachable`/`ValidationFailed`/`NotFound`/`Conflict`/`Unauthorized`/`Cancelled`/`Internal`/`Unknown`, plus per-reason factories). The prescribed `IActionLogger` is **deferred** — when an action first needs structured logging, inject `ILogger<TAction>` from `Microsoft.Extensions.Logging.Abstractions` directly (already the test-guidance default; no bespoke wrapper).

2. **Composition seam** ([SiloActionsServiceCollectionExtensions.cs](../src/UX/Weave.Actions/SiloActionsServiceCollectionExtensions.cs)). Frontends call `services.AddSiloActions(client => client.BaseAddress = …)` once. The extension registers a typed `HttpClient` per action class (`AddHttpClient<TAction>(configureClient)`) so each action gets its own `HttpClient` injected by .NET's `IHttpClientFactory` — automatic disposal, automatic handler pooling, no shared singleton client. New actions add one `AddHttpClient<>` line each.

3. **Pilot action** ([SystemInfo/GetSystemInfoAction.cs](../src/UX/Weave.Actions/SystemInfo/GetSystemInfoAction.cs)). Ctor takes `ISystemConfigSource` (frontend-supplied snapshot of `~/.weave/config.json`-shaped data: `Version`, `BaseUrl`, `DefaultPort`, `Storage`, `AuthMode`, `RequireHttps`, `SiloPath`, `WeaveHome`) plus `HttpClient`. `ExecuteAsync` reads the snapshot, probes `GET /health` directly via the typed client (2-second linked timeout, swallows the standard `HttpRequestException`/`TaskCanceledException`/`SocketException` triplet → `Reachable = false`), and returns the combined `SystemInfoResult`. The folder is `SystemInfo/` rather than `System/` — the latter shadows `global::System.*` for any file in the namespace, so renaming up front avoided a scattered `global::` tax.

4. **Phase 1 verbs** ([Agent/](../src/UX/Weave.Actions/Agent/), [Tool/](../src/UX/Weave.Actions/Tool/), [AgentTask/](../src/UX/Weave.Actions/AgentTask/), [Workspace/](../src/UX/Weave.Actions/Workspace/), [Config/](../src/UX/Weave.Actions/Config/), [Dashboard/](../src/UX/Weave.Actions/Dashboard/)). Silo-bound verbs take `HttpClient`, call a `GET/POST /api/...` endpoint, deserialize into a private `internal sealed record XxxWire` (defined in `XxxJsonContext.cs` alongside the per-feature source-gen `JsonSerializerContext`, camelCase + case-insensitive to match the silo's wire), then map to a curated public summary record. Folders: `Agent/` (ListAgentsAction → `AgentSummary { AgentName, Status, Model, ActiveTasksCount, ConnectedToolsCount }`); `Tool/` (ListToolsAction → `ToolSummary { ToolName, ToolType, Status, Endpoint, ConnectedAt, ErrorMessage }`); `AgentTask/` (ListTasksAction → `TaskSummary { TaskId, Description, Status, CreatedAt, CompletedAt }`); `Workspace/` (GetWorkspaceStatusAction + ValidateWorkspaceAction). The folder is `AgentTask/` rather than `Task/` — the latter shadows `System.Threading.Tasks.Task` for any file in the namespace, same gotcha as `SystemInfo/` vs `System/`. Each Wire→Summary translation is one ~10-line loop inside the action. The two local-only verbs (`Config/GetConfigAction`, `Dashboard/GetDashboardStatusAction`) skip the wire-DTO machinery: `GetConfigAction` reads only from `ISystemConfigSource` and returns a `ConfigSummary` with all formatting (`int → decimal`, `bool → "true"/"false"`, `string? SiloPath → "(not set)"`) baked in so frontends just print strings; `GetDashboardStatusAction` takes both `ISystemConfigSource` (for the URL fallback when the caller doesn't supply one — silo port → dashboard port pairing via `WeavePorts`) and `HttpClient` (for the reachability probe via absolute URI, so the typed client's silo BaseAddress is unused but harmless).

5. **CLI bindings** ([src/UX/Weave.Cli/ActionContext/](../src/UX/Weave.Cli/ActionContext/) + `Commands/{System,Agents,Tools,Tasks,Config,Workspace}/`). `ConsoleActionPrompter` wraps Spectre `TextPrompt`/`SelectionPrompt`/`AnsiConsole.Confirm`; `ConsoleActionReporter` routes through `CliTheme.Write{Info,Success,Warning,Error}`; `CliSystemConfigSource` reads `CliConfigStore.Load()` and folds in the resolved silo base URL plus the `~/.weave` home. `SystemCliCommand`/`SystemCommand` wire `weave system`. `AgentsCliCommand`/`AgentsCommand` wire `weave agents [workspace]`; `ToolsCliCommand`/`ToolsCommand` wire `weave tools [workspace]`; `TasksCliCommand`/`TasksCommand` wire `weave tasks [workspace] [agent]`. `ConfigGetCliCommand`/`ConfigGetCommand` wire `weave config get [key]`; the rendering goes through the shared `ConfigSummaryRenderer` so `weave config get` (no key, full table) and the TUI `/config` view both print the same eight rows. `WebUiCliCommand`/`WebUiCommand` wire `weave webui` — this command keeps its OS-specific browser-launch side effect (`Process.Start` with the platform-conditional fallback to `xdg-open`/`open`); the action only resolves the URL and probes reachability. The CLI command reads `WEAVE_WEBUI_URL` itself before calling the action so the action stays env-var-agnostic. All commands use the standard guided + advanced modes; agents and tools fall back to manifest-only rendering when the silo is unreachable. Tasks is silo-only (tasks are an agent-runtime concept, not declared in the manifest). `AgentsRenderer` / `ToolsRenderer` / `TasksRenderer` / `ConfigSummaryRenderer` / `SystemInfoRenderer` (internal static) hold the Spectre table layouts shared between each CLI command and the migrated TUI views. DI all in [`CliServiceCollection`](../src/UX/Weave.Cli/CliServiceCollection.cs); the lone `services.AddSiloActions(c => c.BaseAddress = new Uri(CliApiHttp.ResolveBaseUrl()))` call wires every action's `HttpClient` (config-only actions register as transient via `AddTransient<>` since they don't need a typed client).

6. **TUI fully migrated for Phase 1 reads.** `/agents`, `/tools`, `/tasks`, `/config`, `/webui`, `/system` are all on the action layer: `TuiAgentListView` / `TuiToolsView` / `TuiTasksView` consume their respective actions and bottom out at the same renderers as the CLI commands; `TuiConfigView` (instance class) takes `GetConfigAction` and renders through `ConfigSummaryRenderer`; `TuiSystemView` (instance class) takes `GetSystemInfoAction` and renders through `SystemInfoRenderer`; `/webui` flows through the DI-resolved `WebUiCliCommand`. `TuiAgentListView` keeps a manifest-only fallback for the workspace-not-running case (TUI-specific UX concession the action layer doesn't need to model); `TuiToolsView` and `TuiTasksView` are silo-only — they short-circuit when `!session.IsRunning`. `TuiWorkspaceDashboard` (the dashboard rendered on TUI start and on `/dashboard`) also drained — its silo-reachability stat now reads `result.Reachable` from `GetSystemInfoAction` rather than calling `TuiRuntimeProbe.ProbeSiloAsync(...)` (probe class deleted; was a one-line wrapper around the same `/health` GET). The previous PR #62 shape — `TuiToolsView`/`TuiTasksView` taking `WorkspaceApiClient` via DI — is gone; the `services.AddSingleton<WorkspaceApiClient>()` registration was dropped along with that migration. **No Phase 1 read-only TUI surface uses `WorkspaceApiClient` anymore.** The remaining inline `new WorkspaceApiClient()` callsites are all on Phase 2 write paths (workspace open/start/stop, chat session, agent name source, data export/import) — they drain as Phase 2 verbs migrate.

7. **`WorkspaceApiClient` stays put.** Plain `internal sealed class WorkspaceApiClient : IDisposable` in `Weave.Cli.Commands` — no `Weave.Actions` interface, no translation layer. It serves the two remaining inline `new WorkspaceApiClient(...)` callsites (`Commands/Data/{DataExport,DataImport}CliCommand.cs`), both of which fan out across skills, channels, templates, and marketplace endpoints — fully draining them needs new action verbs in those domains, deferred to a later batch. The static helpers `WorkspaceApiClient.PrepareManifest` (relative→absolute path resolution before POSTing the manifest) and `WorkspaceApiClient.GetWorkspaceStatePath` (locates `.weave/workspace-id` next to the manifest) live on the same class and are still consumed by `WorkspaceUpCliCommand` / `TuiWorkspaceStarter` / `RunCliCommand` / the data import path — they're not action-layer concerns (path resolution is frontend orchestration; the silo sees absolute paths only) and stay where they are.

8. **Phase 2 first batch (start/stop) shipped.** [`Workspace/StartWorkspaceAction`](../src/UX/Weave.Actions/Workspace/StartWorkspaceAction.cs) takes a prepared `WorkspaceManifest` (frontend reads + parses + path-resolves) and POSTs to `/api/workspaces`; the request envelope is `StartWorkspaceWire { WorkspaceManifest Manifest }` declared in the per-feature `WorkspaceJsonContext` (camelCase, `WhenWritingNull`). The 201 response deserializes through the existing `WorkspaceWire` and translates to a `WorkspaceStatusSummary` (same record returned by `GetWorkspaceStatusAction` — silo emits `WorkspaceResponse` for both endpoints). Failure mapping: 400 → `ValidationFailed` with the silo's per-key errors map joined as `"{key}: {msg}"`, 409 → `Conflict` carrying the silo's RFC 7807 `detail`, 401/403 → `Unauthorized`, 5xx → `Internal`. [`Workspace/StopWorkspaceAction`](../src/UX/Weave.Actions/Workspace/StopWorkspaceAction.cs) takes a `WorkspaceId` string and DELETEs `/api/workspaces/{Uri.EscapeDataString(id)}`. Today's silo-side contract is "204 (NoContent) or 409 (Conflict via `InvalidOperationException` catch); `WorkspaceActor.StopAsync` no-ops when not Running so 409 doesn't actually fire under current actor logic" — the action's switch arms handle 409 + 401/403/5xx defensively but skips a `NotFound` arm because the silo never 404s on stop. Adding `Weave.Workspaces` ProjectReference back to `Weave.Actions` so `WorkspaceManifest` source-gens through the per-feature JSON context (validate's earlier removal of that ref was specific to its case — it sends manifest text as a JSON string; start sends the typed object). DI: `services.AddHttpClient<StartWorkspaceAction>` + `<StopWorkspaceAction>` lines in `AddSiloActions`.

   Frontend wiring: `WorkspaceUpCliCommand` ctor now takes `StartWorkspaceAction` + `GetSystemInfoAction` (the latter replaces the inline `WorkspaceApiClient.IsReachableAsync` probe — `GetSystemInfoAction` already does the same `/health` GET). The silo auto-start flow stays put: probe via the action; if not reachable, `WorkspaceSiloStarter.AutoStartServeAsync(ct)` (frontend orchestration, separate from the silo HTTP). `TuiWorkspaceStarter` flipped from `new TuiWorkspaceStarter(agentSelector)` (no-action ctor) to `(agentSelector, StartWorkspaceAction, GetSystemInfoAction)` — TuiShell still inlines the `new` because `TuiAgentSelector` itself isn't DI-resolved. `WorkspaceDownCliCommand` keeps the `IWorkspaceDownDependencies` testability seam introduced in earlier work; `DefaultWorkspaceDownDependencies.StopWorkspaceAsync` is now a one-line forward to `stopAction.ExecuteAsync(...)` returning `Task<ActionResult<StopWorkspaceResult>>` — the down command switches on the result instead of catching a translated `InvalidOperationException`, with the catch list reduced to `IOException`/`UnauthorizedAccessException` (the only things the local `DeleteFile` cleanup can throw). `TuiWorkspaceStopper` flipped from `internal static` to instance class taking `WorkspaceDownCliCommand`; `TuiSlashCommandDispatcher` ctor + `_workspaceStopper` field gain that param. `WorkspaceUpCommand`/`WorkspaceDownCommand` factories now take handler args mirroring the existing pattern (`WorkspaceStatusCommand.Create(handler)` etc.); `Program.cs` and `CliCommandStructureTests` updated to pass DI-resolved instances. `CliServiceCollection` registers `WorkspaceUpCliCommand`, `WorkspaceDownCliCommand`, and `IWorkspaceDownDependencies → DefaultWorkspaceDownDependencies` as transients. **Inline `new WorkspaceApiClient()` count: 8 → 5 with this PR** (`WorkspaceUpCliCommand`, `TuiWorkspaceStarter`, `DefaultWorkspaceDownDependencies` drained).

Tests ([src/UX/Weave.Actions.Tests/](../src/UX/Weave.Actions.Tests/)): 101 total (was 80). `Context/ActionResultTests.cs` (4) covers `ActionResult.Success`/`Failed` shape, `ActionFailure` factory reasons, the `Cancelled()` default message. `SystemInfo/GetSystemInfoActionTests.cs` (5) drives `GetSystemInfoAction` against a `StubHttpMessageHandler` covering health-200, health-500, `HttpRequestException`-throws-during-probe, `/health`-endpoint-targeting, and the null-input guard. `Agent/ListAgentsActionTests.cs` (7), `Tool/ListToolsActionTests.cs` (7), and `AgentTask/ListTasksActionTests.cs` (10) cover wire-JSON-translation, empty-array, cancellation, `HttpRequestException → SiloUnreachable`, escaped-id-endpoint-targeting, null-input, and blank-identifier `[Theory]` rows (Tasks adds 4 rows for the workspace+agent matrix). `Workspace/GetWorkspaceStatusActionTests.cs` (8) and `Workspace/ValidateWorkspaceActionTests.cs` (10) cover the silo-bound workspace verbs including 404→`NotFound`, `EnsureSuccessStatusCode → SiloUnreachable`, and the various 4xx/5xx mapping branches. `Workspace/StartWorkspaceActionTests.cs` (10) covers 201-success + translation, 400-validation with the per-key errors-map join + the no-errors fallback default, 409-conflict with the `<<MARKER-FROM-SILO>>` token discipline that ensures the assertion can't pass for the wrong reason + the no-detail fallback, 401-Unauthorized, 500-Internal, `HttpRequestException → SiloUnreachable`, endpoint-targeting (`/api/workspaces`), null-input. `Workspace/StopWorkspaceActionTests.cs` (11) covers 204-success, 409-conflict-with-detail + no-detail-fallback, 401-Unauthorized, 500-Internal, `HttpRequestException → SiloUnreachable`, escaped-id-endpoint-targeting, an explicit `HttpMethod.Delete` assertion, null-input, and a blank-id `[Theory]` (2 rows). `Config/GetConfigActionTests.cs` (16) covers the case-insensitive single-key `[Theory]` (10 rows for the 8 keys plus 2 case-folded duplicates), unknown-key validation failure with key-list hint, full snapshot, RequireHttps rendering, null-SiloPath rendering, cancelled token, and null input. `Dashboard/GetDashboardStatusActionTests.cs` (11) covers default-port URL computation (DashboardHttp), non-default port (DefaultPort+1), explicit URL, whitespace fallthrough, the five HTTP status reachability boundaries (200/404/401/500/502), the `HttpRequestException → Reachable=false` swallow, and the null-input guard. The shared `Helpers/StubHttpMessageHandler.cs` is the reusable test seam — `Returns(status, body)` and `Throws(ex)` factories plus a `LastRequestUri` capture for endpoint-targeting assertions.

## Next work

**Phase 1 read-only verbs are fully shipped, including all TUI consumers.** All seven planned reads (`ListAgentsAction`, `ListToolsAction`, `ListTasksAction`, `GetWorkspaceStatusAction`, `ValidateWorkspaceAction`, `GetConfigAction`, `GetDashboardStatusAction`) are in place; the CLI and TUI both consume them; `TuiSystemView` and `TuiWorkspaceDashboard` migrated off `TuiRuntimeProbe`/`WorkspaceApiClient` to consume `GetSystemInfoAction` directly; `TuiRuntimeProbe.cs` deleted. The locked Phase 0 decisions held up across all eight verbs (one Phase 0 pilot + seven Phase 1 reads). Five shared `Renderer` classes (`AgentsRenderer`/`ToolsRenderer`/`TasksRenderer`/`ConfigSummaryRenderer`/`SystemInfoRenderer`) are the dedup'd table-rendering seam between each CLI command and its TUI counterpart.

**Phase 2 — write verbs. Complete.** All seven planned write verbs are shipped: `StartWorkspaceAction`, `StopWorkspaceAction`, `SendMessageAction`, `SetConfigAction`, `OpenWorkspaceAction`, `SelectAgentAction`, `WatchWorkspaceAction`. `weave up` / `weave down` / TUI `/up` / `/down` / inline TUI chat / `weave run` / `weave config set` / TUI `/open` / TUI `/use` / TUI `/watch` / `TuiLiveStatusView` all consume actions. Six inline `new WorkspaceApiClient()` callsites drained across the run-pre-Phase-2 reads + the four Phase 2 batches: `WorkspaceUpCliCommand`, `TuiWorkspaceStarter`, `DefaultWorkspaceDownDependencies` (start/stop); `TuiChatSession`, `TuiAgentNameSource` (chat/agent-list); `RunCliCommand` (run-command). Inline `new WorkspaceApiClient()` count: 8 → 5 → 3 → 2 (`Commands/Data/{DataExport,DataImport}CliCommand.cs` remain — both touch skills/channels/templates/marketplace endpoints which need their own action sets before draining; deferred to a separate batch).

**Phase 3 — three-project split. Shipped.** `Weave.Cli` is now three projects: `Weave.Cli.Shell` (shared shell primitives — theme, config readers/writers, workspace registry, version service, action-consuming `*CliCommand` handlers that both `weave …` invocations and the TUI dispatch through), `Weave.Cli.Tui` (interactive TUI shell + slash dispatcher + composer + per-verb views), and `Weave.Cli` (System.CommandLine command tree + `Program.cs` + composition root + non-shared CLI handlers). Project ref direction: `Weave.Cli.Tui → Weave.Cli.Shell + Weave.Actions`; `Weave.Cli → Weave.Cli.Shell + Weave.Cli.Tui + Weave.Actions`. The action contract is now the only legal seam between CLI invocations and the TUI in the build graph.

**Phase 3.5 — services masquerading as static helpers. Shipped.** Five new interfaces in `Weave.Cli.Shell` (`IConfigStore`, `ISecretResolver`, `IWorkspaceRegistry`, `IManifestResolver`, `ISiloLauncher`) replace the six pre-existing static classes. `WorkspaceSiloStarter` and `WorkspaceSiloPaths.ResolveSiloPath` fused into `SiloLauncher` since both belong to the same launching responsibility (the only static remnant of `WorkspaceSiloPaths` is `GetSiloLogPath()` — pure path math, no deps). `WorkspacePrompt` also flipped from static to instance (Spectre prompt + `IWorkspaceRegistry` + `IManifestResolver` deps); the one truly-pure method (`WriteManifestNotFound`) stays as a static helper on the same class. `SiloProcessService` flipped to an instance class with `IConfigStore` + `ISecretResolver` for the storage/auth-arg builders, with the pure-function members (`IsReachableAsync`, `WaitForReadyAsync`, `BuildSiloArgs`, `TryKill`, `AttachLogDrainer`) staying static. The two `CliConfigStore` static delegations to `CliSecretResolver` (`ResolveConnectionString`, `ToEnvReference`) dropped — call sites that need secret resolution inject `ISecretResolver` directly.

Workspace-name completion — the only `CliCompletions` method that needed a runtime dep — moved to a new `WorkspaceCompletions(IWorkspaceRegistry)` class in `Weave.Cli.Commands`. The compile-time-data completions (`CompletePresetNames`, `CompleteDeployTargets`, `CompleteToolTypes`, `CompleteRuntimeTypes`, `CompletePluginTypes`, `CompleteConfigKeys`) stay on the static `CliCompletions`. Each `*Command.Create()` factory grew one parameter (`WorkspaceCompletions` for the 17 sites that hand workspace-name completion to a `CompletionSources.Add` call); `Program.cs` resolves it once and threads it through.

DI rewire: every `*CliCommand` handler is now resolved from `CliServiceCollection`. `Program.cs` grew `services.GetRequiredService<XCliCommand>()` calls for each of `RunCliCommand`, `ServeCliCommand`, `InitCliCommand`, `WorkspaceListCliCommand`, `WorkspaceRemoveCliCommand`, `WorkspaceNewCliCommand`, `WorkspaceShowCliCommand`, the four `WorkspaceAdd*CliCommand`s, `WorkspacePublishCliCommand`, `WorkspacePresetsCliCommand`, the two `WorkspaceStorage*` and the two `Storage*` handlers, `WorkspacePluginListCliCommand`, `WorkspacePluginRemoveCliCommand`, `DataExport/ImportCliCommand`, the six `Marketplace*CliCommand`s, `AuditReplayCliCommand`, `PortsCliCommand`, `VersionCliCommand`. `XCommand.Create()` factories accept the handler (and `WorkspaceCompletions` / `IConfigStore` where needed) instead of `new XCliCommand()`-ing inline. The pre-existing migrated handlers (status/validate/up/down/agents/tools/tasks/config) absorbed `IManifestResolver` and `WorkspacePrompt` ctor params.

The TUI side: `TuiShell` ctor gained `ISiloLauncher` + `IWorkspaceRegistry` + `IManifestResolver` + `PortsCliCommand`; the `new TuiSession()` call became `new TuiSession(_manifestResolver)`. `TuiWorkspaceStarter` ctor gained `ISiloLauncher`; its `WorkspaceSiloStarter.AutoStart…` calls flipped to `_siloLauncher.AutoStart…`. `TuiWorkspaceDashboard` ctor gained `IWorkspaceRegistry`. `TuiSlashCommandDispatcher` ctor gained `PortsCliCommand` so the inline `new PortsCliCommand()` (which would have failed once the type required `IConfigStore`) became `_portsCommand.ExecuteAsync(...)`.

Tests: 2122 total — 2113 passed, 9 Docker-skipped. +5 from Phase 3.5: `ManifestResolverTests` (3 — registered-name with manifest, registered-name without manifest, unknown-name) and `WorkspaceCompletionsTests` (2 — registry contents pass-through, empty registry). Existing `CliSecretResolverTests` flipped from `CliSecretResolver.X(...)` static calls to `new CliSecretResolver()` instance form (CA1859 forced concrete-type field rather than `ISecretResolver` interface for the test). `WorkspaceDownCliCommandTests` (5) and `WorkspacePublishCommandTests` (1 ctor call) flipped to the new ctor signatures via tiny `EmptyRegistry` / `NullManifestResolver` stubs co-located in the test file. `CliCommandStructureTests` updated for the new `*Command.Create(handler, …)` signatures across all the migrated factories. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` punch list (one fix applied): WARN — initial extraction of `registry.Register(...)` from `ScaffoldWorkspaceAsync` (MarketplaceInstall) and `RestoreFilesAsync` (DataImport) up to the caller silently moved the call from "after CreateDirs, before WriteManifest" to "after the helper returns" — a real behaviour delta on partial-failure paths. Fix: passed the field-injected registry into the helper (via `private async` → `this`), restoring original ordering. Net code delta: 504 insertions / 636 deletions across 91 files (mostly the Program.cs / CliServiceCollection.cs growth offset by the explicit-ctor-to-primary-ctor compaction in pre-existing handlers).

The classes that pass the codified test (pure-function, ≥3 consumers) stay static: `WorkspaceManifestPaths` (path math), `StatusMarkup` (string formatting), `CliTheme` (constants + formatters), `WorkspacePresets` (compile-time data), `CliApiHttp` (env + config flat-read), `WorkspaceSiloPaths.GetSiloLogPath` (one-line `Path.Combine`), `CliCompletions` (compile-time data, six methods), `WorkspacePrompt.WriteManifestNotFound`, `SiloProcessService.{IsReachableAsync,WaitForReadyAsync,BuildSiloArgs,TryKill,AttachLogDrainer}` (pure utilities the instance class also uses).

**Phase 4b — drain DataExport/DataImport, delete `WorkspaceApiClient`. Shipped.** Six new action verbs in `Weave.Actions/`: `Skill/{ListSkills,PostSkill}Action`, `Channel/{ListChannels,PostChannel}Action`, `Template/ListTemplatesAction`, `Marketplace/ListMarketplaceItemsAction`. All return / accept opaque `JsonElement` for wire fidelity — the only callers today are workspace export/import roundtrips that need to preserve any silo-emitted field unchanged. A future user-facing "list skills" command can introduce a typed `SkillSummary` action without affecting these. `DataExportCliCommand` migrated off `WorkspaceApiClient` + `MarketplaceApiClient` to the new actions plus the existing `GetSystemInfoAction` (reachability) — `WorkspaceExport` lost the diagnostic `Agents` and `Tools` fields (never read by import; silo regenerates from manifest) and flipped `MarketplaceItems` from typed `ApiMarketplaceItemResponse` to `JsonElement[]` to match the other lists. `DataImportCliCommand` migrated to `StartWorkspaceAction` + `PostSkillAction` + `PostChannelAction` + `GetSystemInfoAction`. **`WorkspaceApiClient.cs` deleted**, along with 8 orphaned wire DTOs (`ApiAgentResponse`, `ApiToolResponse`, `ApiTaskResponse`, `ApiWorkspaceResponse`, `ApiStartWorkspaceRequest`, `ApiSendMessageRequest`, `ApiChatResponse`, `ApiConversationMessage`); `CliApiJsonContext` trimmed to just the `Marketplace*` and `Audit*` types still used by `MarketplaceApiClient` and the audit handler.

**Phase 4c — drain `MarketplaceApiClient` + `AuditApiClient`. Shipped.** Eight new typed actions in `Weave.Actions/{Marketplace,Audit}/`: `BrowseMarketplaceItemsAction`, `SearchMarketplaceAction`, `GetMarketplaceItemAction`, `SubmitMarketplaceItemAction`, `PublishMarketplaceItemAction`, `InstallMarketplaceItemAction` (typed `MarketplaceItemSummary` + `InstalledMarketplaceTemplate` — distinct from Phase 4b's opaque-JsonElement `ListMarketplaceItemsAction` which stays for export fidelity); `GetCapabilityAuditByTokenAction`, `GetRecentCapabilityAuditAction` (typed `CapabilityAuditEntry`). The 6 marketplace commands and `MarketplaceItemPrompt` migrated; `AuditReplayCliCommand` migrated. **Both seam classes deleted** along with 6 orphaned wire DTOs and the now-empty `CliApiJsonContext`. `CliApiHttp.EnsureSuccessOrThrowAsync` deleted (dead code without the seams). Every CLI command that talks to the silo now goes through a typed action — zero seam-class callsites remain.

**Phase 4a — TUI verb registry. Shipped.** New `ITuiVerb` interface + `TuiVerbContext(Session, Args, ClearChatHistory)` record in `Weave.Cli.Tui/Verbs/`. Every per-verb helper that was doing real work (`TuiWorkspaceOpener`, `TuiAgentSelector`, `TuiAgentListView`, `TuiWorkspaceStarter`, `TuiWorkspaceWatcher`, `TuiToolsView`, `TuiTasksView`, `TuiConfigView`, `TuiSystemView`) now implements `ITuiVerb` directly — primary-ctor + `DispatchAsync(TuiVerbContext, ct)` instead of bespoke per-verb method signatures. Bare `ICliCommand` callsites get one thin verb wrapper each in `Verbs/`: `StatusVerb`, `ValidateVerb`, `DownVerb` (absorbs the previously-existing `TuiWorkspaceStopper` logic, which is now deleted), `WebUiVerb`, `UpgradeVerb`, `PortsVerb`, `VersionVerb`, `PresetsVerb`, `RefreshVerb`. Each wrapper is 11–25 lines.

`TuiSlashCommandDispatcher` rewrote: from 19 ctor seams + 23-arm switch to **2 ctor seams** (`TuiChatSession` + `IEnumerable<ITuiVerb>`) + a 6-arm meta-verb switch (`help`/`quit`/`clear`/`history`/`new` — these touch dispatcher-state directly and don't fit the verb shape) + a registry lookup. File dropped 215 → 81 lines. `TuiShell` lost the inline `new TuiAgentListView(...)` / `new TuiWorkspaceOpener(...)` / etc. constructions — every helper is DI-resolved through the registry. `TuiShell` ctor seams: 13 → 7. `TuiWorkspaceStopper.cs` deleted (its 29-line body lives in `DownVerb` now).

DI registration: each `ITuiVerb` impl registered both as its concrete type (where any other helper or `TuiShell` references it directly) AND via `services.AddTransient<ITuiVerb, X>()` so DI's `IEnumerable<ITuiVerb>` resolution picks them up. `TuiAgentSelector` and `TuiWorkspaceDashboard` keep concrete-type registrations because `TuiWorkspaceOpener` / `TuiWorkspaceStarter` (for `TrySelectOnlyAgent`) and `TuiShell.RunAsync` (for startup refresh) reference them outside the registry path.

**LLM streaming — Phase 1 (provider + middleware). Shipped.** `AnthropicChatClient.GetStreamingResponseAsync` no longer stubs over the non-streaming path — it sets `stream: true` on the request body, sends with `HttpCompletionOption.ResponseHeadersRead`, and consumes the SSE response through `AnthropicSseParser` (line-buffered reader emitting `(event, data)` tuples per W3C SSE spec) → `AnthropicStreamingMapper` (event-to-`ChatResponseUpdate` state machine). Text deltas yield one `ChatResponseUpdate` per `content_block_delta` text frame; the final update carries `UsageContent` (M.E.AI's streaming-usage convention) + `FinishReason`. `event: error` mid-stream now throws `InvalidOperationException` with the error payload — was silently swallowed in the first cut, caught by adversarial-review. The middleware decorators got matching streaming overrides: `CostTrackingChatClient.GetStreamingResponseAsync` aggregates usage from `UsageContent` items as updates fly by and records once at the end; `RateLimitingChatClient.GetStreamingResponseAsync` acquires the token-bucket lease before yielding the first update, then passes through.

`Microsoft.Extensions.AI.OpenAI`'s `AsIChatClient()` already streams SSE natively via the official OpenAI SDK — no Phase 1 work needed for that provider. `FallbackChatClient.GetStreamingResponseAsync` stays a no-op (it's the unconfigured-agent fallback, no real LLM call to stream).

Files added: `AnthropicSseParser.cs` (49 lines), `AnthropicStreamingMapper.cs` (110 lines), `StreamingStubChatClient.cs` (test helper). Wire records for `message_start` / `content_block_delta` / `message_delta` events co-located in `AnthropicMessagesJsonContext.cs` with their three new `[JsonSerializable]` entries (matches the "wire DTO co-located with its context" rule from Phase 0). `AnthropicChatClient.MapFinishReason` lifted to `AnthropicStreamingMapper.MapFinishReason` so both streaming and non-streaming paths share the same `stop_reason` → `ChatFinishReason` mapping.

Tests: +27 across five new files. `AnthropicSseParserTests` (7: single event, multi-event, multi-line data, comment lines, missing-space-after-colon, no-trailing-blank-line, empty stream). `AnthropicStreamingMapperTests` (10: full sequence, empty-text-delta-not-yielded, non-text-delta-ignored, no-usage-final-update, modelId-fallback-to-default, unknown-event-ignored, `[Theory]` for stop-reason mapping × 4 rows, unknown-stop-reason-null, error-event-throws-with-marker). `AnthropicChatClientStreamingTests` (5: deltas-in-order, final-update-has-usage-and-finish-reason, request-body-has-stream-true, null-messages-throws, options-model-id-overrides). `CostTrackingChatClientStreamingTests` (4: records-once-on-final, no-usage-no-record, passes-through-updates, later-overwrites-earlier). `RateLimitingChatClientStreamingTests` (2: within-limit-passes-through, exceeds-limit-throws). Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` punch list (one fix applied): BLOCK — `error` SSE event silently swallowed; added `case "error": throw` with the error payload in the message.

**LLM streaming — Phase 2 (pipeline). Shipped.** `AgentChatPipeline.ExecuteStreamingAsync` returning `IAsyncEnumerable<AgentChatStreamingFrame>` — a discriminated-union of `AgentChatTextFrame(string Text)` for per-delta text fragments and `AgentChatCompleteFrame(AgentChatResponse Response)` for the terminal frame carrying the full `AgentChatResponse` (content + conversation-id + messages + used-tools + model). `IAgentChatPipeline.ExecuteStreamingAsync` added to the interface. The preamble + finalization logic that used to live inline in `ExecuteAsync` is now split into two private helpers — `BuildRequestAsync` (user-message append, system-prompt resolution, user-context / skill-memory / episodic-memory enrichment, tool build, ChatOptions assembly, returns a private `ChatRequest` record bundling `Messages` + `Options` + `SkillIds` + `EpisodeIds`) and `FinishAsync` (conversation-id update, assistant-message history append, last-active timestamp, skill/episodic recall recording, returns the final `AgentChatResponse`). Both sync `ExecuteAsync` and the new streaming variant call the same helpers; `ExecuteAsync` body collapsed to three lines (build → call → finish). The streaming variant accumulates `ChatResponseUpdate`s in a list, yields one `AgentChatTextFrame` per non-empty `TextContent` delta, then combines via `IEnumerable<ChatResponseUpdate>.ToChatResponse()` (M.E.AI's terminal combiner) and runs `FinishAsync` exactly once before yielding the terminal `AgentChatCompleteFrame`.

Tests: +7 in `AgentChatPipelineStreamingTests`. Text-frame ordering (`Hello, ` / `world` / `!` in order, terminal frame last, exactly-one terminal frame), terminal-frame concatenated content (`"Hello, world!"` + model id), user-message-history-append-before-first-yield (asserts inside the foreach with a `<<USER-MARKER>>` substring), assistant-message-history-append-on-completion, advances-last-active-after-streaming-completes (captures LastActive at first frame, advances `FakeTimeProvider` by 30s mid-stream, asserts the post-completion timestamp is strictly later — adversarial-review caught the original `ShouldBeGreaterThanOrEqualTo(before)` as pass-for-wrong-reason since `BuildRequestAsync` already sets `LastActive` from the user-message timestamp), no-text-deltas emits only a complete frame with empty content, empty-text-content not yielded as a TextFrame (verifies the `{ Length: > 0 }` pattern match). All 28 pre-existing `ExecuteAsync` tests pass against the refactored implementation (the BuildRequest/Finish extraction is a behavior-preserving refactor verified by the existing suite). Build clean (0 warnings, 0 errors). `check-rules`: WARN — `AgentChatPipeline.cs` at 237 lines is 37 over the 200 guideline; design check accepts as-is (one cohesive pipeline responsibility, sync+async paths share five private helpers, splitting would force instance state out as parameters). `adversarial-review` punch list (one fix applied): WARN — the LastActive test mirrored the pre-existing pattern that passes whether `FinishAsync` updates the timestamp or not; tightened with mid-stream `FakeTimeProvider.Advance` so the assertion fails if the `FinishAsync` update is removed.

Reused `StreamingStubChatClient` test helper from Phase 1 — extracting it to its own file in Phase 1 paid off on the first reuse.

**LLM streaming — Phase 3a (server side). Shipped.** Added `IAsyncEnumerable<AgentChatStreamingFrame> SendStreamingAsync(AgentMessage, CancellationToken)` to `IAgentActor` + `AgentActor` (same lifecycle gate as `SendAsync`; persists state after streaming completes via `CancellationToken.None` so a mid-stream caller-cancellation still flushes the user-message append). Forwarded through `AgentActorGrain` bridge. New silo endpoint `POST /api/workspaces/{id}/agents/{name}/messages/stream` returning `text/event-stream` with two event types: `event: text` carrying `{"text":"..."}` per delta and `event: complete` carrying the full `ChatResponse` JSON for the terminal frame. Endpoint probes the first `MoveNextAsync` inside a try/catch so lifecycle errors (`InvalidOperationException` from the actor when agent isn't active) surface as a regular 409 *before* any SSE header is written. Validation failures (empty content, oversize) return JSON 400 unchanged from the unary path. Endpoint bypasses CQRS — the streaming transport shape doesn't fit the `ICommandHandler<T,TResult>` pattern, and `SendAgentMessageHandler` was already a 3-line grain forward.

Orleans serialization for `AgentChatStreamingFrame`: the silo's `SiloBuilderConfigurator.ConfigureSerializer` already registers `AddJsonSerializer` for every `Weave.*` (non-`Weave.Silo.*`) type, so the abstract base + 2 derived records round-trip across the grain boundary via STJ. The tagged union got `[JsonPolymorphic(TypeDiscriminatorPropertyName = "$type")]` + `[JsonDerivedType(typeof(AgentChatTextFrame), nameof(AgentChatTextFrame))]` markers so STJ can reify the abstract base.

`FallbackChatClient.GetStreamingResponseAsync` flipped from `yield break` to yielding one `ChatResponseUpdate` per content type — a text update for the synthesized response text, then a usage update with the (pseudo) token counts. Was a no-op when no pipeline consumer existed; now `AgentChatPipeline.ExecuteStreamingAsync` is the consumer and the integration tests need the fallback to behave like a (collapsed) streaming provider. Same shape as the production providers, fewer chunks. Side cleanup: dropped the `ILogger<FallbackChatClient>` ctor parameter that only fed the now-deleted "streaming not supported" log line; `ProviderResolver.Fallback` lost the matching `loggerFactory` argument.

Tests: +6 in `AgentStreamingEndpointTests` (SSE content-type, event ordering `["text","complete"]`, text-event payload carries the `<<USER-MARKER>>` substring, complete-event payload carries the marker in `content` + non-empty `model`, agent-not-activated → 409, empty content → 400). `FallbackChatClientTests.GetStreamingResponseAsync_ReturnsEmpty` rewrote as `_YieldsTextAndUsageUpdates` (2 updates: text containing user input, usage non-empty). Full suite: 2334 passed / 9 Docker-skipped. Build clean (0 warnings, 0 errors). `check-rules` NOTE-only (3 marginal file-size signals: `AgentActor.cs` 273 → 287 + 14 lines for SendStreamingAsync; `FallbackChatClient.cs` 198 → 212 over the 200 threshold by 12 lines; pre-existing `AgentChatStreamingFrame.cs` 3-records-per-file tagged-union exception). `adversarial-review` clean.

**LLM streaming — Phase 3b (action). Shipped.** New `SendMessageStreamingAction` in `src/UX/Weave.Actions/Agent/`. `IAsyncEnumerable<SendMessageStreamingChunk> StreamAsync(SendMessageInput, CancellationToken)` POSTs to `/api/workspaces/{id}/agents/{name}/messages/stream` with `HttpCompletionOption.ResponseHeadersRead` (so the response body is read incrementally as bytes arrive — without this `HttpClient` buffers the entire SSE response first), parses each `event: text` / `event: complete` frame, and yields chunks until terminal. Chunks are a 3-way tagged union: `SendMessageTextChunk(Text)`, `SendMessageCompleteChunk(SendMessageResult)`, `SendMessageErrorChunk(ActionFailure)`. After an error or complete chunk the action stops reading; the silo's contract guarantees every successful 200 ends in `event: complete`, so a stream that ends without one yields an `Internal` error chunk ("Silo closed the stream without a complete event"). Failure-status mapping mirrors `SendMessageAction`: 400 → `ValidationFailed` (joins per-key RFC 7807 errors), 409 → `Conflict` (silo `detail` or fallback), 401/403 → `Unauthorized`, 5xx + unexpected → `Internal`, `HttpRequestException` → `SiloUnreachable`, caller-side cancellation → `Cancelled`.

Local W3C-spec SSE line reader (`SseLineReader`, ~35 lines) lives next to the action. Sibling parser at `Weave.Agents.Pipeline.Providers.Anthropic.AnthropicSseParser` for the upstream LLM connection. They aren't shared because actions can't pull pipeline internals (CLAUDE.md dep-flow rule) and the W3C format is small enough that two copies beats a shared abstraction — both files document the other in `<remarks>`. Local `StreamingTextWire` wire record added to `AgentJsonContext` (matches the silo's `TextEventWire`).

DI: registered via `services.AddHttpClient<SendMessageStreamingAction>(configureSiloClient)` next to the unary `SendMessageAction`.

Tests: +12 in `SendMessageStreamingActionTests` — happy path (2 text + 1 complete with translated result), terminal-after-complete (stream ends at complete even if more events follow), missing-complete → `Internal` error, 400/409/401/500/HttpRequestException all map to the right `ActionFailureReason`, URL-template + escaping test asserts exact path `/api/workspaces/ws%2F1/agents/agent%20name/messages/stream`, null-input and blank-identifier throw `ArgumentException`. Test stub uses a local `SseHandler : HttpMessageHandler` that emits a canned SSE body with `text/event-stream` Content-Type. Marker substrings (`<<MARKER-FROM-SILO>>`, `<<NETMARK>>`) prevent assertions from passing through fallback paths.

Net 2334 → 2346 (+12). Build clean. `check-rules` WARN-only on `SendMessageStreamingAction.cs` 237 lines (one class + 6 private helpers, single responsibility — splitting would create coordination indirection); `adversarial-review` clean.

**LLM streaming — Phase 3c (TUI consumer). Pending.** `TuiChatSession.SendAsync` rewritten: drop the `AnsiConsole.Status` spinner, call `SendMessageStreamingAction.StreamAsync`, pattern-match each chunk — text chunks → write to the console as they arrive; complete chunk → replace local history with the carried `SendMessageResult.Messages` and render the model footer; error chunk → switch on `Failure.Reason` and render with the matching CLI theme verb. The user echo + footer wrapper from the unary path is preserved. When this lands, the TUI will visibly stream tokens for Anthropic-configured agents (the only provider with real streaming today; OpenAI/Fallback collapse to one text frame followed by complete).

The original Shape C rationale below stays in place for reference — the structure of Phase 2+ is unchanged.

**Original Shape C rationale (unchanged from PR #62 plan).** A step-back review of the TUI piece (during PR #62) concluded its structure is wrong: 23 slash commands duplicate ~22 CLI commands, both reach into `WorkspaceApiClient` directly, the TUI helpers (`TuiWorkspaceOpener`, `TuiAgentSelector`, etc.) couple Spectre prompts inline so no other frontend can reuse the orchestration. A future web dashboard, LSP integration, or any non-Spectre frontend would have to rewrite all of it. The fix is to extract a shared **Actions layer** between the silo HTTP API and the frontends — both CLI and TUI become consumers of the same actions, neither hosts the other.

### What an "Action" is

A unit of frontend orchestration: gather input, call the silo HTTP API, render the result. **Not** business logic — that lives in silo CQRS handlers and actors. Actions are the layer above those, the layer **below** any specific frontend.

```text
Input  — typed record, what the caller already knows
Context — IActionPrompter (ask user) + IActionReporter (progress) + IActionLogger
         — frontends provide concrete impls; actions never know which
Result — typed record OR ActionFailure with a structured reason
```

An action **never** writes to `Console`, **never** uses `AnsiConsole`, **never** reaches into a session, **never** knows whether it's running inside a CLI invocation or a TUI slash command. It calls `WorkspaceApiClient`, asks the prompter for missing input, reports progress through the reporter, and returns data.

### Project layout

```
src/UX/
├── Weave.Actions/                    NEW — frontend actions, no I/O for prompts/rendering
│   ├── Context/                      IActionPrompter, IActionReporter, IActionLogger, ActionResult<T>, ActionFailure
│   ├── Workspace/                    Start, Stop, Open, Watch, Validate, GetStatus, Dashboard
│   ├── Agent/                        Select, ListAgents, ListTools, ListTasks, SendMessage
│   └── System/                       GetSystemInfo, GetConfig, SetConfig
├── Weave.Cli/                        consumes Weave.Actions
│   ├── ActionContext/                ConsoleActionPrompter, ConsoleActionReporter (Spectre + batch-mode default)
│   └── Commands/                     each CLI command: build input, resolve action, render result
├── Weave.Cli.Tui/                    NEW project, split out from Weave.Cli (Phase 3)
│   ├── ActionContext/                TuiActionPrompter, TuiActionReporter (Spectre.Status spinner)
│   ├── Shell/                        loop, composer, session
│   ├── Dispatch/                     parser, dispatcher, single generic /verb-to-action adapter
│   └── Rendering/                    TUI-specific result renderers
└── Weave.Dashboard/                  eventually consumes Weave.Actions in-process
```

### Migration sequence

1. **Phase 0 (Foundation) + Phase 1 first verb — SHIPPED this session (see "How Shape C is wired today" above).** `Weave.Actions` project with Context primitives (`IActionPrompter`/`IActionReporter`/`ActionResult<T>`/`ActionFailure`/`ActionFailureReason`), the `AddSiloActions` composition extension, `GetSystemInfoAction` pilot, `ListAgentsAction` (first Phase 1 verb), and 16 unit tests. CLI bindings (`ConsoleActionPrompter`/`ConsoleActionReporter`/`CliSystemConfigSource`/`AgentsRenderer`), `weave system` and `weave agents` commands, TUI `/agents` migrated. Conservative-path attempt (`ISiloApi` interface + `WorkspaceApiClient` translation) was tried mid-session and **rolled back in the same commit** in favour of actions-own-HTTP — see "Decisions locked" below for why.

2. **Phase 1 — Read-only verbs. SHIPPED, including all TUI consumers.** All seven planned verbs landed: `ListAgentsAction`, `ListToolsAction`, `ListTasksAction`, `GetWorkspaceStatusAction`, `ValidateWorkspaceAction`, `GetConfigAction`, `GetDashboardStatusAction`. Both CLI and TUI consume them; every Phase 1 TUI surface (`TuiAgentListView`, `TuiToolsView`, `TuiTasksView`, `TuiConfigView`, `TuiSystemView`, `TuiWorkspaceDashboard`) consumes its action directly. `TuiRuntimeProbe.cs` deleted (was a one-line `WorkspaceApiClient.IsReachableAsync` wrapper duplicating `GetSystemInfoAction`'s `/health` probe). The locked template (typed `HttpClient` for silo-bound, `ISystemConfigSource` for local-only) held up across all seven verbs.

3. **Phase 2 (2-3 PRs) — Write verbs.** `OpenWorkspaceAction`, `SelectAgentAction`, `StartWorkspaceAction`, `StopWorkspaceAction`, `WatchWorkspaceAction`, `SendMessageAction`, `SetConfigAction`. The prompting-heavy cases where `IActionPrompter` earns its keep.

4. **Phase 3 (1 PR) — Split `Weave.Cli.Tui` from `Weave.Cli`.** Forces the action contract as the only legal seam between the two projects.

5. **Phase 4 (1-2 PRs) — Drain residue.** Delete the per-verb TUI helper classes (replaced by a single generic `/verb` → action adapter). Drain the remaining inline `new WorkspaceApiClient()` callsites — by this point all action-using code paths get the singleton via DI.

### Decisions locked by Phase 0 + Phase 1 first verb

1. **Three injected services vs one bundle: three separate.** `IActionPrompter` and `IActionReporter` are real interfaces in `Weave.Actions/Context`. `IActionLogger` is **not** introduced — actions that need logging take `ILogger<TAction>` directly. The three-services principle stands; the third slot is just the standard Microsoft.Extensions.Logging interface.
2. **Typed result `ActionResult<TResult>`.** `readonly record struct` with `[MemberNotNullWhen]` on `IsSuccess` so the compiler tracks Value/Failure non-nullability through the success/failure branches. Constructed via the non-generic `ActionResult.Success<T>(value)` / `ActionResult.Failed<T>(failure)` factory — type-inferred at the call site, no CA1000 hit on a generic-static.
3. **Action method signature: `Task<ActionResult<T>> ExecuteAsync(TInput input, CancellationToken ct)`.** Context comes via DI on the action class, not as a method parameter. Verified by `GetSystemInfoAction` and `ListAgentsAction`.
4. **`Weave.Cli.Tui` stays a folder until Phase 3** — splitting projects mid-migration adds friction.
5. **Silo CQRS handlers stay separately named** (server-side); frontend actions are a different layer despite similar shapes.
6. **Actions own their HTTP. No shared "silo client" abstraction.** Each action takes a typed `HttpClient` (registered via `AddHttpClient<TAction>(configureClient)` inside `Weave.Actions`'s own `AddSiloActions` extension) and owns its endpoint URL, JSON deserialization, and error mapping. Why: the conservative path (a `WorkspaceApiClient`-like seam in `Weave.Actions`) requires either promoting the existing `internal sealed` CLI silo client + 11 wire DTOs to `public` (locks in a public API surface that's actually a private CLI/silo HTTP contract) or wrapping it in a growing `ISiloApi` interface that accumulates a translation slice per verb. The first verb shipped, was reviewed, and rolled back in the same commit when the translation tax became visible. Each action having one ~10-line wire-record + per-feature `JsonSerializerContext` is cheaper than the seam.
7. **Per-feature source-gen `JsonSerializerContext`.** One `JsonSerializerContext` per feature folder (`AgentJsonContext.cs` lives next to `ListAgentsAction.cs`), with the wire DTO co-located in the same file. Silo response-shape changes ripple to one folder, not a centralized DTO module.
8. **`WorkspaceApiClient` is not on the migration path.** It stays plain `internal sealed class WorkspaceApiClient : IDisposable` in `Weave.Cli.Commands` for the unmigrated 12 callsites (was 13 before this session's `TuiAgentListView` migration). Each remaining callsite drains as its consumer becomes an action; the client deletes itself when the last callsite migrates. No "lift to `Weave.Actions`" event ever happens.
9. **No `System` sub-namespaces inside `Weave.Actions` or its tests.** `Weave.Actions.System` and `Weave.Actions.Tests.System` shadow `global::System.*` for every file in those namespaces, requiring scattered `global::` qualifiers. Folder is `SystemInfo/` instead. Same rule applies to any future verb folder — pick names that don't collide with .NET's top-level namespaces.

### What's already on disk

Shape C Phase 0 shipped this session (see above + History). PR #62 shipped the original handoff item (the two `TuiToolsView`/`TuiTasksView` hidden-dep fix) but **deliberately did not** continue the larger TUI refactor it explored. Five exploratory commits (dispatcher one-interface refactor + 23 command classes + 10 helper interfaces + WorkspaceApiClient Singleton) were force-rolled-back as wrong-direction once Shape C was identified. Don't re-attempt that path; Phase 1 builds on the action layer that landed in Phase 0.

### Pre-existing items, both blocked on production signal

1. **Operator runbook for rotation.** The signing-key rotation plumbing is still documented only in the XML doc on `CapabilityTokenOptions.PreviousSigningKey`. Promote to `docs/security.md` once a real rotation is exercised end-to-end.

2. **Audit-row dead-letter shape.** The retry loop is bounded; if `weave.silo.audit.write_failures{outcome="dropped"}` ever fires in production a row is gone for good. The bounded-queue + dead-letter log shape called out in earlier handoffs is still the right move *if* the dropped-rate metric proves non-zero in real use. Wait for the signal before implementing.

### Small follow-ups, not blocking

- *(Closed: `weave marketplace install --no-scaffold` / `--workspace-name X` flags shipped in the 2026-05-08 follow-up batch.)*
- **Custom-flow path through the primitive.** `WorkspaceNewTemplateFactory.CreateCustom` still builds the `assistant` agent inline (no template to compose from). If we ever invent a "naked template" for the no-preset case, we could route the custom flow through the same primitive too — but that's invented work, wait until a real shape forces it.

## Cross-cutting follow-ups

These remain real gaps. None blocks the *Next work* items above.

- **`channel:send:*` requires both grants today** because `RouteInboundAsync` does both ingress and reply atomically. The double `Authorize` call now lives at the call site (lines 74–75 of ChannelGatewayActor) with distinct `actionContext` strings (`":receive"` / `":send"`), so the audit log distinguishes them. If a webhook adapter ever needs receive-only, split the actor surface.

- **Tell-don't-ask state-record pattern: decided.** Carve-out codified in [docs/best-practices.md → Tell-don't-ask on state records](best-practices.md). Methods earn their place when there's multi-field internal coupling (eviction + frequency counting, score-multiple-fields query, lifecycle state machine, distinct-list normalization) — codified exemplars: `SkillMemoryState.Search`, `EpisodicMemoryState.Recall`, `AgentState.{SubmitTask,…}`, `UserProfileState.{RecordInteraction,BuildContextSummary,Clear}`, `ToolRegistryState.{IsToolAllowed,GrantTools,ConfigureAccess}`, `ChannelGatewayState.ResolveAgent`. Methods don't earn their place when they'd just wrap a one-line `Dict.TryGetValue` — codified passive bags: `AgentSupervisorState`, `VerifierState`. Future state records consult the rule.

## Hygiene queue

Wave 5 vertical-slice splits drained this session (see History). Items remaining:

**Subsumed by Shape C migration** — don't drain piecemeal:
- The remaining two inline `new WorkspaceApiClient(...)` callsites (`Commands/Data/{DataExport,DataImport}CliCommand.cs`) both fan out across skills/channels/templates/marketplace endpoints — fully draining them needs new action verbs in those domains. Drained so far: PR #62 (`TuiToolsView`, `TuiTasksView`); Phase 0+1 sweep (`TuiAgentListView`, `TuiLiveStatusView`, `TuiLiveStatusWatcher`, `TuiSystemView`, `TuiWorkspaceDashboard`, `WorkspaceStatusCliCommand`, `WorkspaceValidateCliCommand`, `TuiRuntimeProbe`-deleted); Phase 2 start/stop batch (`WorkspaceUpCliCommand`, `TuiWorkspaceStarter`, `DefaultWorkspaceDownDependencies`); Phase 2 chat/agent-list batch (`TuiChatSession`, `TuiAgentNameSource`); Phase 2 run-command batch (`RunCliCommand`). Pattern: each callsite's consumer becomes an action taking `HttpClient`, the inline `new` deletes, the action takes the same DI place.
- `Weave.Cli/Commands/Workspace/*Command.cs` (15+ files) all use the `static class XCommand { Create() }` pattern with `cmd.SetAction((_, ct) => new XCliCommand().ExecuteAsync(...))`. These get drained as their CLI logic moves into Actions in Phases 1-2. The new `Commands/{System,Agents}/{System,Agents}Command.cs` keep the same shape — System.CommandLine builders calling a DI-resolved handler, identical to the existing `UpgradeCommand`/`TuiCommand` pattern; Phases 0–1 did not invent a new CLI command shape, only a new layer below it.

**Independent of Shape C:**
- *(Closed: long XML doc summaries.)* The 22 `<summary>` blocks over 5 lines were split into `<summary>` + `<remarks>` this session. The detector in `check-rules` cat 11 was upgraded from "count contiguous `///` lines" to "count content inside `<summary>...</summary>`," so future audits won't false-positive on legitimate `<remarks>`.

## Template to mirror

For the next vocabulary entry (or any follow-up that touches the audit pipeline):

| What | Where |
|---|---|
| Single authorizer with Authorize signature | `src/Security/Weave.Security/Tokens/CapabilityAuthorizer.cs` |
| Audit event record | `src/Security/Weave.Security/Events/CapabilityAuthorizationEvent.cs` |
| In-memory audit store + bounded options | `src/Security/Weave.Security/Audit/InMemoryCapabilityAuditStore.cs` |
| Subscriber hosted service | `src/Runtime/Weave.Silo/Audit/CapabilityAuditSubscriberHostedService.cs` |
| Per-actor call-site shape | `await authorizer.AuthorizeAsync(token, grant, actorWorkspaceId, actionContext);` |
| DI seam | `SiloServiceRegistrar.RegisterSecurity()` (store) + `RegisterAgentPipeline()` (hosted service) |
| Query records + handlers | `src/Security/Weave.Security/Queries/Get*CapabilityAuditQuery.cs` |
| HTTP endpoint group | `src/Runtime/Weave.Silo/Api/AuditEndpoints.cs` (mapped in `SiloApplicationConfigurator.MapEndpoints`) |
| CLI command pair | `src/UX/Weave.Cli/Commands/Audit/AuditReplayCliCommand.cs` + `AuditReplayCommand.cs` (wired in `Program.cs`) |
| End-to-end audit test | `src/Runtime/Weave.Silo.Tests/Audit/CapabilityAuditEndpointTests.cs` |

## History

- **2026-05-11** (`claude/loop-next-feature-aBApX`) — **LLM streaming — Phase 3b: `SendMessageStreamingAction`.** New action returning `IAsyncEnumerable<SendMessageStreamingChunk>` that POSTs to the silo SSE endpoint with `HttpCompletionOption.ResponseHeadersRead`, parses W3C-spec SSE frames, and yields a 3-way tagged-union of chunks (Text / Complete / Error). Failure-status mapping mirrors `SendMessageAction` (400 → ValidationFailed, 409 → Conflict, 401/403 → Unauthorized, 5xx → Internal, `HttpRequestException` → SiloUnreachable, cancellation → Cancelled). Terminal-error invariant: after yielding `SendMessageErrorChunk` or `SendMessageCompleteChunk` the action stops reading; a stream that ends without a complete event yields an Internal error chunk because the silo's contract guarantees one on every successful 200. Local `SseLineReader` (~35 lines) lives next to the action — sibling to `AnthropicSseParser` but actions layer can't pull pipeline internals, so we duplicate rather than abstract. +12 tests in `SendMessageStreamingActionTests` with marker substrings (`<<MARKER-FROM-SILO>>`, `<<NETMARK>>`) to prevent test passes via fallback paths. Net 2343 → 2355 / 9 skipped (2346 actually passing; the slot now reads 2346 because Phase 3b raised the total). Build clean. check-rules WARN on the action file at 237 lines (single responsibility — borderline, left); adversarial-review clean. Phase 3c (TUI consumer) is the explicit next step.

- **2026-05-11** (`claude/loop-next-feature-aBApX`) — **LLM streaming — Phase 3a: server-side SSE endpoint + grain streaming.** Added `IAsyncEnumerable<AgentChatStreamingFrame> SendStreamingAsync` to `IAgentActor` (same lifecycle gate as `SendAsync`; persists state with `CancellationToken.None` after streaming completes so a mid-stream caller-cancellation still flushes). Forwarded through `AgentActorGrain` — first Orleans grain method in the codebase returning `IAsyncEnumerable<T>`. Validated this works through the silo's existing `AddJsonSerializer` (registered for every `Weave.*` non-`Weave.Silo.*` type in `SiloBuilderConfigurator.cs`); no Orleans surrogates needed beyond `[JsonPolymorphic]` + `[JsonDerivedType]` markers on the tagged-union base for STJ to round-trip the abstract type. New endpoint `POST /api/workspaces/{id}/agents/{name}/messages/stream` returning `text/event-stream` with two event types (`text` carrying `{"text":"..."}` per delta, `complete` carrying the full `ChatResponse` for the terminal frame). Lifecycle errors surface as 409 *before* any SSE header is written via a probe-MoveNextAsync-inside-try/catch pattern; validation failures return 400 like the unary path. Endpoint bypasses CQRS (the streaming shape doesn't fit `ICommandHandler<T,TResult>`).

  `FallbackChatClient.GetStreamingResponseAsync` flipped from `yield break` to yielding one text update + one usage update — was a no-op when no pipeline consumer existed; now `AgentChatPipeline.ExecuteStreamingAsync` consumes it and the integration tests need the fallback to behave like a (collapsed) streaming provider. Side cleanup: dropped the `ILogger<FallbackChatClient>` ctor parameter that only fed the now-deleted "streaming not supported" log line; `ProviderResolver.Fallback` lost the matching `loggerFactory` argument.

  Tests: +6 in `AgentStreamingEndpointTests` covering content-type, event ordering, payload contents (with `<<USER-MARKER>>` substring), 409 on inactive agent, 400 on empty content. `FallbackChatClientTests.GetStreamingResponseAsync_ReturnsEmpty` rewrote as `_YieldsTextAndUsageUpdates`. Net 2337 → 2343 (+6). Build clean. `check-rules` NOTE-only (3 marginal file-size signals; no behavior smells). `adversarial-review` clean (skipped Cat 2 — server-side only, no UX render paths). Phase 3b (action) + 3c (TUI consumer) are explicit follow-ups — the silo wire shape is now stable for the action to consume.

- **2026-05-11** (`claude/loop-next-feature-aBApX`) — **LLM streaming — Phase 2: pipeline streaming via `AgentChatPipeline.ExecuteStreamingAsync`.** Added `IAsyncEnumerable<AgentChatStreamingFrame>` to the pipeline interface — discriminated-union with `AgentChatTextFrame(Text)` for per-delta fragments and `AgentChatCompleteFrame(Response)` for the terminal frame. `ExecuteAsync` refactored to share preamble + finalization with the streaming path via two private helpers (`BuildRequestAsync` returning a `ChatRequest` record bundling `Messages` + `Options` + `SkillIds` + `EpisodeIds`; `FinishAsync` taking the `ChatResponse` and doing conversation-id update + assistant-message append + last-active timestamp + skill/episodic recall recording). Streaming variant accumulates `ChatResponseUpdate`s in a list, yields one `AgentChatTextFrame` per non-empty `TextContent` delta, combines via `IEnumerable<ChatResponseUpdate>.ToChatResponse()`, runs `FinishAsync`, yields the terminal frame. Phase 1's `StreamingStubChatClient` test helper reused — extracting it to its own file in the prior commit paid off on the first reuse.

  Tests: +7 in `AgentChatPipelineStreamingTests` (text-frame ordering, terminal-frame concatenation, user-history-before-first-yield with `<<USER-MARKER>>` substring, assistant-history-on-completion, advances-last-active-after-streaming-completes with mid-stream `FakeTimeProvider.Advance`, no-deltas-empty-complete, empty-text-not-yielded). All 28 pre-existing `ExecuteAsync` tests pass against the refactored implementation. 2330 → 2337. Build clean (0 warnings, 0 errors). `check-rules`: WARN — `AgentChatPipeline.cs` 237 lines, 37 over the 200 guideline; design check resolves to keep as-is. `adversarial-review` punch list (one fix applied): WARN — Cat 4 — initial `LastActive` test asserted `ShouldBeGreaterThanOrEqualTo(before)`, but `BuildRequestAsync` already sets `LastActive` to the user-message timestamp before the LLM call, so the test passed whether or not `FinishAsync` updated it; rewrote to capture LastActive at the first yielded frame, advance `FakeTimeProvider` by 30s, then assert the post-completion timestamp is strictly later. Phase 3 (silo HTTP transport + new action + TUI consumer) is the remaining piece for visible token streaming in the chat surface.

- **2026-05-11** (`claude/loop-next-feature-aBApX`) — **LLM streaming — Phase 1: real Anthropic SSE + middleware streaming overrides.** Replaced `AnthropicChatClient.GetStreamingResponseAsync`'s one-shot stub (which yielded a single `ChatResponseUpdate` after calling the non-streaming path) with a real SSE consumer: extracted `AnthropicSseParser` (W3C-spec line-buffered reader emitting `(event, data)` tuples — handles multi-line `data:`, comment lines, missing-space-after-colon, no-trailing-blank-line) and `AnthropicStreamingMapper` (event-to-`ChatResponseUpdate` state machine — `message_start` captures id/model/input-tokens, `content_block_delta` yields per-text-frame updates, `message_delta` accumulates stop-reason + output-tokens, `message_stop` yields a final update with `UsageContent` + `FinishReason`, `error` throws with the payload). Request body gained `Stream` field (`bool?`, null-default so non-streaming requests don't serialize it); streaming path uses `HttpCompletionOption.ResponseHeadersRead` so the SSE body isn't pre-buffered. `MapFinishReason` lifted from `AnthropicChatClient` into `AnthropicStreamingMapper` so both paths share the same `stop_reason` → `ChatFinishReason` mapping. `CostTrackingChatClient` and `RateLimitingChatClient` got streaming overrides: cost tracking aggregates `UsageContent.Details` as updates fly by and records once at the end; rate limiting acquires the token-bucket lease before yielding the first update. `Microsoft.Extensions.AI.OpenAI`'s `AsIChatClient()` already streams SSE natively, so no OpenAI-side change. `FallbackChatClient`'s streaming method stays a no-op (unconfigured-agent fallback). Phase 2 (pipeline streaming method) and Phase 3 (silo HTTP transport + new action + TUI consumer that visibly streams tokens) are explicit follow-ups — see *Next work*.

  Tests: +27 across `AnthropicSseParserTests` (7), `AnthropicStreamingMapperTests` (10 incl. `[Theory]` for stop-reason mapping × 4 rows and error-event-throws-with-`<<MARKER-ERROR>>` substring), `AnthropicChatClientStreamingTests` (5 incl. `\"stream\":true` request-body assertion), `CostTrackingChatClientStreamingTests` (4 incl. later-usage-overwrites-earlier), `RateLimitingChatClientStreamingTests` (2). Shared `StreamingStubChatClient` test helper extracted to its own file. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` punch list (one fix applied): BLOCK — Cat 1 — `case "error":` silently swallowed; Anthropic emits `event: error` for mid-stream failures (server crashes during generation) and the consumer would see "stream ended" with no signal. Added `throw new InvalidOperationException(...)` with the raw error payload + a regression test asserting the error message reaches the caller.

- **2026-05-09** (`claude/next-task-jKret`) — **Security-review hardening on `IsSafeWorkspaceName` + `SanitizeForEcho` echo guard.** Three findings from the security review on the 4-commit branch: (1) **Windows drive-relative path bypass** — the previous `IndexOfAny(['/', '\\'])` check let `C:foo` (no separator, just a drive prefix) through; on Windows `Path.GetFullPath("C:foo")` resolves to drive C:'s CWD, escaping the intended scaffold root. Tightened to `!Path.IsPathRooted(workspaceName) && IndexOfAny(['/', '\\', ':']) < 0` so any `:` character (drive prefix, alternate-data-stream form `foo:bar`) and any rooted path is rejected. (2) **Windows reserved device names** — `CON`/`PRN`/`AUX`/`NUL`/`COM1`-`COM9`/`LPT1`-`LPT9` interact with devices on Windows even when used as directory names; reserved across the codebase regardless of host OS so a workspace bundle is portable. The check matches the part-before-the-first-dot (so `CON.tar.gz` is also rejected, mirroring Microsoft's documented rule), case-insensitive via `StringComparer.OrdinalIgnoreCase`. (3) **Terminal-escape echo** — the rejection error message previously echoed user input verbatim (`$"Invalid workspace name '{workspaceName}'..."`); a hostile name carrying ANSI escape sequences would write them to stdout. Added `SanitizeForEcho` which replaces every `char.IsControl` char with `?` (covers C0/C1 control ranges + DEL); the error message now sanitizes the echoed substring before formatting. Adversarial-review caught one self-finding on the initial impl: `stackalloc char[value.Length]` for unbounded user input is a stack-overflow risk → switched to a fast-path early return for control-free inputs and `string.Create` for the rewrite path otherwise.

  Tests: 4 new path-traversal Theory rows (`C:foo`, `C:`, `D:..\\evil`, `foo:bar`); new `_WindowsReservedNames_AreRejected` Theory (12 rows including case-insensitive `con`/`Nul` and `CON.txt`/`nul.tar.gz` extension forms); 4 new `SanitizeForEcho` Facts (printable-passthrough, ANSI ESC `` replaced, multi-control NL+TAB+BEL `` replaced, DEL `` replaced — using C# `\uXXXX` escape syntax in the source rather than raw control bytes). 2215 → 2235 (+20). Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` clean (one finding self-applied: stackalloc bound).

- **2026-05-09** (`claude/next-task-jKret`) — **Three more UX hardening fixes + a CI-flake doc entry; SiloFactory disposal-hardening attempted and reverted.** Continuation of the prior PR's punch list. (1) `WorkspaceUpCliCommand` post-success state-file write switched to `CancellationToken.None`: previously `await File.WriteAllTextAsync(statePath, workspace.WorkspaceId, ct)` would propagate `OperationCanceledException` if the user hit Ctrl-C between silo-side success and local state recording, leaving the silo running but no local id to use for `weave down` later. The action's success path is the wrong place to honor cancellation. (2) `WorkspaceUpCliCommand` health-probe Cancelled-suppression: `GetSystemInfoAction.ExecuteAsync` swallows `TaskCanceledException` and returns `Reachable=false`, so a Ctrl-C during the 2-second probe would fall into the auto-start branch and print "Server not running — starting automatically..." → spurious silo launch attempt. Added `if (ct.IsCancellationRequested) return 130;` immediately after the systemInfoAction call. (3) `MarketplaceInstallCliCommand` workspace-name path-traversal guard: `--workspace-name X` is passed straight to `Path.GetFullPath(X)` which happily resolves `../../etc` to a directory outside CWD; added a `private static IsSafeWorkspaceName` check that rejects anything containing `/` or `\` or equal to `.` / `..`. The check fires AFTER the prompt fallthrough so it covers both flag-supplied and prompt-supplied names — Spectre prompts can return arbitrary user input, not just the default. (4) `docs/best-practices.md` → "Categories and speed" gained an entry on the **Microsoft.Testing.Platform shutdown-crash flake**: when CI's parent `Build & Test` step exits non-zero but the published `Test Results` check shows zero failures, the orchestrator is propagating a worker disposal crash that fired AFTER the TRX was published; the run is functionally green and the action is "re-run before treating the diff as the cause" rather than `--ignore-exit-code` which hides a real bug. Diagnosed from PR #68's first CI run.

  **Attempted but reverted: SiloFactory disposal hardening.** Tried adding `IAsyncLifetime` + an explicit `await Services.GetRequiredService<IHost>().StopAsync(timeout)` in a `DisposeAsync` override on `SiloFactory : WebApplicationFactory<Program>`, intending to give the per-class fixture a known-bounded shutdown window before xunit's cleanup. The override double-disposed the host (the base `WebApplicationFactory<T>.DisposeAsync` already drives `host.StopAsync` internally), which made the previously-rare "Test Class Cleanup Failure" flake fire on >50% of `Weave.Silo.Tests` runs. Reverted the override; kept a small XML-doc cleanup that splits the SiloFactory `<summary>` block (12 content lines, over the cat-11 threshold) into `<summary>` + `<remarks>`. **Conclusion: the shutdown-crash root cause is somewhere inside the in-process host's hosted-service shutdown sequence (Orleans cluster, ASP.NET host, validators, IChatClient background tasks); without a reliable local repro the safe action is to leave the documented signal-and-retry mitigation in place rather than risk masking a real bug with a swallow-everything override.** Reproduced the flake locally maybe 3-4 times across a 30-run loop with no override; the override consistently turned a 10% flake into a 50% one.

  Tests stable at 2184 / 2175 passed / 9 Docker-skipped — no new tests this commit (the `CancellationToken.None` swap and ct-check timing are awkward to unit-test without a substantial refactor of the Up command to inject a file-system seam; deferred). Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` clean.

- **2026-05-08** (`claude/next-task-jKret`) — **Down/Up UX-conflation NOTEs drained + `weave marketplace install` script-friendly flags + four follow-on UX hardening fixes.** Two commits, one branch. **Commit 1** — three follow-ups: (1) `WorkspaceDownCliCommand` now suppresses the Cancelled message: `if (result.Failure.Reason == ActionFailureReason.Cancelled) return 130;` mirrors the locked exit code from `WorkspaceUpCliCommand` / `MarketplaceInstallCliCommand` (130 = SIGINT). (2) The `DeleteFile`-failure render is split from the silo-stop failure: success message moved up to fire as soon as `StopWorkspaceAsync` returns success; the `catch (Exception ex) when (ex is IOException or UnauthorizedAccessException)` body now writes `"Could not remove local state file '{statePath}': {ex.Message}"` as a Warning and returns 0 — the silo IS stopped, the local cleanup is incidental, and the previous `WriteError("Failed to stop workspace: ...")` lied about which step failed. (3) `weave marketplace install` gained `--no-scaffold` (record the install only, skip workspace scaffolding) and `--workspace-name X` (skip the prompt, use the supplied name). `MarketplaceInstallOptions` extended with `bool NoScaffold = false, string? WorkspaceName = null` defaults so the existing prompt-only path is unchanged when neither flag is passed. The `--no-scaffold` short-circuit happens after the install-API call but before the workspace-name prompt, so install-count metrics still increment. **Commit 2** — four follow-on hardening fixes: (4) `WorkspaceUpCliCommand` now suppresses the silo-auto-start failure message on Ctrl-C: `if (ct.IsCancellationRequested) return 130;` after `siloLauncher.AutoStartServeAsync(ct)` returns false — Ctrl-C during silo startup no longer renders as "Could not start the Weave server. Start it manually with: weave serve". (5) `MarketplaceInstallCliCommand` treats blank `--workspace-name` (`""`, `"  "`) as if the flag wasn't passed, falling through to the existing prompt instead of crashing in `Path.GetFullPath`. The `string.IsNullOrWhiteSpace`-guarded ternary replaces the previous `?? prompt`. (6) `MarketplaceInstallCliCommand` now emits a one-line `WriteWarning("--workspace-name is ignored when --no-scaffold is set.")` when both flags are passed — the adversarial-review NOTE on commit 1 said silent winning was acceptable, but a Warning is honest without being a validation arm and is still trivially scriptable (warnings go to stdout in the CLI's theme, scripts that only check exit codes are unaffected). (7) `WorkspaceDownCliCommand` differentiates blank `--workspace-id` from blank state-file content: `--workspace-id ""` / `"   "` now prints `"--workspace-id is empty."`; an empty state file prints `"Workspace state file '{statePath}' is empty."` — the previous shared message lied about source attribution.

  The deferred Cancelled-suppression NOTE on `WorkspaceUpCliCommand` (the original one, on the `startAction` call site) was already shipped in an earlier Phase 2 commit (line 53-54 of the file does `if (result.Failure.Reason == ActionFailureReason.Cancelled) return 130;`); the new commit-2 fix above is for the *parallel* gap on the auto-start branch.

  Tests: `Weave.Cli.Tests/WorkspaceDownCliCommandTests.cs` gained `ExecuteAsync_StopActionCancelled_Returns130AndLeavesStateFileIntact` (asserts exit 130 + StopAsync ran + state file NOT deleted, proving we exited before cleanup), `ExecuteAsync_DeleteFileFails_ReturnsSuccessSinceSiloStopSucceeded` (asserts exit 0 + StopAsync ran + `DeletedPath` null, proving the catch fired and the throw happened before the stub set the path), and `ExecuteAsync_WithWhitespaceWorkspaceId_ReturnsFailureWithoutReadingStateFile` (`[Theory]` with two rows `""` and `"   "`; asserts `StopCalls.ShouldBe(0)` against `WorkspaceIdText = "from-state-not-read"` — the marker proves the test fails if the code falls through to the state-file branch). Stub `DeleteFile` gained an `Exception? DeleteFileException { get; init; }` knob so the catch path is exercisable. 2179 → 2184 (+5: 3 from commit 1, 2 theory rows from commit 2) + 9 Docker-skipped. Build clean (0 warnings, 0 errors). `check-rules` clean both commits. `adversarial-review` punch list (one fix applied across the two commits): WARN — initial `*_DeleteFileFails_*` test asserted exit code + StopCalls but not `DeletedPath.ShouldBeNull()` → without that assertion the test would still pass if a refactor accidentally removed the try-block entirely; added the assertion so the test now distinguishes "catch fired and swallowed" from "no cleanup attempt at all."

- **2026-05-08** (`claude/continue-handoff-mzGZb`) — **`IWorkspaceDownDependencies` typed-result drain.** The deferred NOTE from the original Phase 2 start/stop PR (`DefaultWorkspaceDownDependencies` translating `ActionFailure` into `InvalidOperationException` so `WorkspaceDownCliCommand` could keep its catch shape) is gone. `IWorkspaceDownDependencies.StopWorkspaceAsync` now returns `Task<ActionResult<StopWorkspaceResult>>`; `DefaultWorkspaceDownDependencies.StopWorkspaceAsync` reduces to a one-line forward to `stopAction.ExecuteAsync(...)`; `WorkspaceDownCliCommand.ExecuteAsync` switches on the result for the silo-call failure (`if (!result.IsSuccess) WriteError($"Failed to stop workspace: {result.Failure.Message}"); return 1;`) and the remaining `try/catch` shrinks to `catch (Exception ex) when (ex is IOException or UnauthorizedAccessException)` for the local `DeleteFile` cleanup — the only call in the try body that can throw either. The previously-needed `HttpRequestException`, `TaskCanceledException`, `SocketException`, `JsonException`, `InvalidOperationException` arms are gone with the translation.

  Tests: `WorkspaceDownCliCommandTests.TestWorkspaceDownDependencies.StopWorkspaceAsync` flipped to return `Task<ActionResult<StopWorkspaceResult>>` (defaults to `ActionResult.Success(new StopWorkspaceResult())` via an init-only `StopResult` property; failure-path tests set it explicitly). New test `ExecuteAsync_StopActionFails_ReturnsFailureAndLeavesStateFileIntact` covers the silo-call failure path: stub returns `ActionFailure.Conflict("Workspace is in a transitional state.")`; assertions check exit code 1, that `StoppedWorkspaceId` was set (proves the silo call ran), and that `DeletedPath` is null (proves the state file was *not* deleted on failure — cleanup only happens on success). 2170 → 2171 (+1) + 9 Docker-skipped. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` clean for this diff; two pre-existing UX-conflation NOTEs surfaced (`Cancelled` reason renders as "Failed to stop workspace: Operation cancelled." rather than silent on Ctrl-C; `DeleteFile` failure renders as "Failed to stop workspace" even though the silo stop already succeeded) — both are unchanged from the original code, orthogonal follow-ups parallel to the deferred Cancelled-suppression NOTE on `WorkspaceUpCliCommand` already noted in the Phase 2 start/stop history entry.

- **2026-05-07** (`claude/next-handoff-task-E4WGX`) — **Tell-don't-ask state-record decision: shipped (carve-out codified + 3 records migrated).** Survey found the previous handoff entry was partially stale: `AgentState` had already been migrated to own its 7 task-lifecycle methods (`SubmitTask`/`FailTask`/`AcceptTask`/`RejectTask`/`SetAwaitingReview`/`RefreshBusyStatus`/computed `RunningTaskCount`); `SkillMemoryState.Search` and `EpisodicMemoryState.Recall` were the originally-cited exemplars. Migrated three more strong-yes records: `UserProfileState` gained `RecordInteraction(record, now)` (eviction + frequency counting + first/last-seen across 4 fields), `BuildContextSummary()` (3-section StringBuilder over preferences + top-5 topics + domain context + total), `Clear()` (10-field reset to defaults). `ToolRegistryState` gained `IsToolAllowed(agent, tool)` (two-step access lookup), `GrantTools(agent, tools)` and `ConfigureAccess(map)` (distinct-list normalization). `ChannelGatewayState` gained `ResolveAgent(channel, message)` (target-agent precedence + routing-rule pattern matching with sender-id-substring + content-prefix fallbacks).

  `UserModelActor`, `ToolRegistryActor`, `ChannelGatewayActor` reduced to thin orchestrators (read state → authorize → call state method → write state → publish event). Actors lost ~80 lines of state-shape-aware logic; state records gained the same. Net LOC roughly flat. The actor's deleted `IsToolAllowed` private method and `ResolveAgentWithRules` internal method are now state-record methods. Test rename: `ResolveAgentWithRules_*` → `ResolveAgent_*` (4 tests) plus `state.ResolveAgent(...)` instead of `actor.ResolveAgentWithRules(...)`; the now-unused `var actor = CreateActor(state);` line in each of the 4 tests dropped during adversarial-review.

  `AgentSupervisorState` and `VerifierState` stay passive — codified as exemplar carve-outs in `docs/best-practices.md`. The new "Tell-don't-ask on state records" subsection (under "Code conventions") gives the rule explicitly: multi-field coupling earns a method; one-line dictionary calls don't. Includes the quick self-check ("if you'd write the body as a single `state.Field.Method(...)` call, leave it on the caller").

  Tests stable at 2179 / 2170 passed / 9 Docker-skipped — pure structural relocation, no behaviour change. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` punch list (one fix applied): WARN — 4 channel-routing tests had unused `var actor = CreateActor(state);` after the migration; dropped. NOTE: `ToolRegistryState.GrantTools` keeps the original `toolNames is null ? [] : ...` defensive null-handling for refactor-as-no-op fidelity; could be dropped in a follow-up since state methods can trust their actor callers (the boundary is at the actor, not the state record).

  **Same session: long XML doc summaries split + detector upgraded.** The previous `check-rules` cat 11 detector counted contiguous `///` lines, which over-counted any file with a legitimate `<remarks>` immediately following `<summary>` — yesterday's audit reported 80 offenders, the real count was 22. Split all 22 across 20 files (`<summary>` keeps the first sentence(s); threading, lifetime, design rationale moved to `<remarks>`). `CapabilityAuthorizer` also dropped a "previously lived in ToolActor / SkillMemoryActor / …" historical reference — exactly the rot pattern flagged by the comment-discipline rule. Detector upgraded to count content lines inside `<summary>...</summary>` only (tracks an `in_sum` flag between the opener and closer; reports `file:line` of the offending opener so the auditor can jump straight to it). Doc-only diff plus skill-md update: build clean (0/0). Detector confirms zero summaries over 5 lines remain.

- **2026-05-07** (`claude/next-handoff-task-E4WGX`) — Shape C **Phase 4c: drain `MarketplaceApiClient` + `AuditApiClient`. Both seam classes deleted.** Eight new typed actions in `Weave.Actions/`: `Marketplace/{BrowseMarketplaceItems,SearchMarketplace,GetMarketplaceItem,SubmitMarketplaceItem,PublishMarketplaceItem,InstallMarketplaceItem}Action` (typed `MarketplaceItemSummary` + `InstalledMarketplaceTemplate` records, distinct from Phase 4b's opaque-JsonElement `ListMarketplaceItemsAction` which stays for workspace export wire fidelity); `Audit/{GetCapabilityAuditByToken,GetRecentCapabilityAudit}Action` (typed `CapabilityAuditEntry` records). Each follows the locked Phase 0 wire→summary translation contract with per-feature JsonContext.

  Migrated callsites: 6 marketplace commands (List/Search/Info/Submit/Publish/Install) plus `MarketplaceItemPrompt` (the static prompt-helper now takes `BrowseMarketplaceItemsAction` instead of `MarketplaceApiClient`); `AuditReplayCliCommand` plus its inner `PromptForTokenIdAsync`. Each command's reachability probe flipped from `client.IsReachableAsync()` to `GetSystemInfoAction.ExecuteAsync` (same `/health` GET). The Install command's `try { client.InstallAsync } catch (InvalidOperationException)` exception-bag dropped in favor of typed `ActionFailureReason.Conflict` — the silo's detail string surfaces verbatim through `ActionFailure.Conflict(detail)`.

  **`MarketplaceApiClient.cs` and `AuditApiClient.cs` deleted.** Six orphaned wire DTOs deleted: `ApiMarketplaceItemResponse`, `ApiMarketplaceInstallResponse`, `ApiMarketplaceInstallTemplate`, `ApiSubmitMarketplaceRequest`, `ApiPublishMarketplaceRequest`, `ApiCapabilityAuditEntry`. `CliApiJsonContext.cs` deleted (no remaining types after Phase 4b's prior trim left only marketplace + audit; both gone now). `CliApiHttp.cs` trimmed to just `ResolveBaseUrl()` (the `EnsureSuccessOrThrowAsync` helper used only by the deleted seams was dead code).

  This closes the Shape C seam-class drain: **zero `WorkspaceApiClient`, `MarketplaceApiClient`, or `AuditApiClient` callsites remain**. Every CLI command that talks to the silo now goes through a typed action with reason-aware failure mapping. `CliApiHttp.ResolveBaseUrl` stays because it's still used by `AddSiloActions` (the typed-HttpClient base URL configurator) and `CliSystemConfigSource`.

  Tests: 2146 → 2179 (+33). New `Weave.Actions.Tests/Marketplace/{Browse,Search,Get,Submit,Publish,Install}MarketplaceItemActionTests.cs` and `Weave.Actions.Tests/Audit/{GetCapabilityAuditByToken,GetRecentCapabilityAudit}ActionTests.cs`. Every test asserts against stub-unique values (`Failure.Message.ShouldBe("Item is not Published.")` for the 409 body roundtrip, `LastRequestUri.PathAndQuery.ShouldBe("/api/marketplace/search?q=git%20tools")` for URL escaping) so no test passes via fallback. Build clean (0 warnings, 0 errors). Full suite green: 2170 / 9 Docker-skipped. `check-rules` clean. `adversarial-review` clean. NOTE recorded: wire-collection-nullability convention (`List<X>?` with `?? []`) is consistent across Phase 1/4b/4c actions; technically defensive on a private contract but an established pattern that would need a repo-wide flip in a separate PR.

- **2026-05-07** (`claude/next-handoff-task-E4WGX`) — Shape C **Phase 4a: TUI verb registry shipped.** First attempt was a "pragmatic" pass that fixed two inline `new` calls and inlined `TuiWorkspaceStopper`, dropping the dispatcher's 17 deps to 19 (net +1 from the new injections). Caught and reversed direction after the user pointed out that 19 ctor seams is the smell, not the cleanup.

  New `ITuiVerb` interface + `TuiVerbContext(TuiSession Session, string? Args, Action ClearChatHistory)` record in `Weave.Cli.Tui/Verbs/`. Each existing per-verb helper that was already doing real work — `TuiWorkspaceOpener`, `TuiAgentSelector`, `TuiAgentListView`, `TuiWorkspaceStarter`, `TuiWorkspaceWatcher`, `TuiToolsView`, `TuiTasksView`, `TuiConfigView`, `TuiSystemView` — flipped to implement `ITuiVerb` directly: primary ctor for deps, `DispatchAsync(TuiVerbContext, ct)` instead of bespoke `OpenAsync(session, arg, callback, ct)` / `RenderAsync(session, ct)` / etc. Bare `ICliCommand` callsites that previously sat as switch arms in the dispatcher gained one thin verb wrapper each in `Verbs/`: `StatusVerb` (HasWorkspace guard + delegate), `ValidateVerb` (same), `DownVerb` (IsRunning guard + delegate + `session.MarkStopped()` on success — absorbs the deleted `TuiWorkspaceStopper`), `WebUiVerb`, `UpgradeVerb`, `PortsVerb`, `VersionVerb`, `PresetsVerb`, `RefreshVerb`. Each thin wrapper is 11–25 lines.

  `TuiSlashCommandDispatcher` rewrote: **19 ctor seams → 2** (`TuiChatSession` + `IEnumerable<ITuiVerb>`); 23-arm switch → 6-arm meta-verb switch (`help`/`quit`/`clear`/`history`/`new` — touch dispatcher-state directly so don't fit the verb shape) + registry lookup. File dropped 215 → 81 lines. `TuiShell` lost the inline `new TuiAgentListView(...)` / `new TuiWorkspaceOpener(...)` / etc. constructions; every helper is DI-resolved through the registry. `TuiShell` ctor seams: 13 → 7. `TuiWorkspaceStopper.cs` deleted.

  DI registration: each `ITuiVerb` impl registered both as concrete type AND via `services.AddTransient<ITuiVerb, X>()` so `IEnumerable<ITuiVerb>` resolution picks them up. `TuiAgentSelector` and `TuiWorkspaceDashboard` keep concrete-type registrations because `TuiWorkspaceOpener` / `TuiWorkspaceStarter` (for `TrySelectOnlyAgent`) and `TuiShell.RunAsync` (for startup refresh) reference them outside the registry path. `RefreshVerb` is a thin wrapper around `TuiWorkspaceDashboard.RefreshAsync` so the dashboard doesn't have to mix verb-shaped and non-verb responsibilities.

  Tests stable at 2146 / 2137 passed / 9 Docker-skipped — pure structural refactor, no behaviour change. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` clean. Note recorded: `TuiAgentSelector` is resolved twice per dispatch (via the registry and via the concrete-type ref in Opener/Starter) since both registrations are Transient — stateless apart from the `_parser` field, so no correctness issue, but a Singleton flip would deduplicate. Not changed in this PR because TUI consistency uses Transient.

- **2026-05-07** (`claude/next-handoff-task-E4WGX`) — Shape C **Phase 4b: drain `DataExport`/`DataImport`, delete `WorkspaceApiClient`.** Six new action verbs in `Weave.Actions/`: `Skill/{ListSkills,PostSkill}Action`, `Channel/{ListChannels,PostChannel}Action`, `Template/ListTemplatesAction`, `Marketplace/ListMarketplaceItemsAction`. All return/accept opaque `JsonElement` for wire-fidelity roundtrip — deliberate carve-out from the Phase 0 wire→summary translation pattern, justified because data-export/import need to preserve silo-emitted fields exactly. Each action sits in its own per-feature folder with a per-feature `JsonContext` (matching the locked Phase 0 layout); each uses primary-ctor `(HttpClient httpClient)` and the standard `ActionResult<T>` failure-mapping shape. `PostSkillResult` / `PostChannelResult` are empty records mirroring the existing `StopWorkspaceResult` precedent — they're carriers for the `ActionResult<T>` generic slot, not a smell.

  `DataExportCliCommand` migrated: `using var client = new WorkspaceApiClient()` and `using var marketplaceClient = new MarketplaceApiClient()` both gone, replaced by ctor-injected `GetSystemInfoAction` (reachability probe), `ListSkillsAction`, `ListChannelsAction`, `ListTemplatesAction`, `ListMarketplaceItemsAction`. The action layer's typed-failure mapping cleaned up the `try { GetXAsync } catch (HttpRequestException or TaskCanceledException or SocketException or JsonException or IOException) { ... }` exception-bag noise — each call is now `if (result.IsSuccess) export.X = result.Value.X; else if (Reason != Cancelled) WriteMuted(...)`. `WorkspaceExport` lost two fields: `Agents` and `Tools` (diagnostic-only — never read by import; silo regenerates runtime state from the manifest), and `MarketplaceItems` flipped from typed `ApiMarketplaceItemResponse[]` to `JsonElement[]` to match the other lists. The on-disk export file format stays JSON-compatible — JsonElement and the typed record serialize the same wire shape.

  `DataImportCliCommand` migrated: `using var client = new WorkspaceApiClient()` gone, replaced by ctor-injected `GetSystemInfoAction` + `StartWorkspaceAction` + `PostSkillAction` + `PostChannelAction`. The single `try { client.StartWorkspaceAsync; foreach skills/channels client.PostXAsync } catch (HttpRequestException or ... ) { ... }` exception-bag dropped in favour of typed-failure mapping at each step. The manifest-parse step keeps a narrow `catch (JsonException or FormatException or ArgumentException)` since the parser is pre-action.

  **`WorkspaceApiClient.cs` deleted** along with 8 orphaned wire DTOs: `ApiAgentResponse`, `ApiToolResponse`, `ApiTaskResponse`, `ApiWorkspaceResponse`, `ApiStartWorkspaceRequest`, `ApiSendMessageRequest`, `ApiChatResponse`, `ApiConversationMessage`. `CliApiJsonContext` trimmed from 19 `[JsonSerializable]` attributes to 9 — kept only the `ApiMarketplaceItem*` / `ApiSubmitMarketplaceRequest` / `ApiPublishMarketplaceRequest` / `ApiCapabilityAuditEntry` / `JsonElement` types still in use. **Inline `new WorkspaceApiClient()` count: 2 → 0.**

  `MarketplaceApiClient` stays for the 6 marketplace command callsites (info/list/search/submit/publish/install) — Phase 4b's scope is the `WorkspaceApiClient` callsites. Draining `MarketplaceApiClient` would need typed `Marketplace{Info,Search,Submit,Publish,Install}Action` verbs and is deferred.

  Tests: 2122 → 2146 (+24). New `Weave.Actions.Tests/{Skill,Channel,Template,Marketplace}/`: `ListSkillsActionTests` (5), `PostSkillActionTests` (5: success/badrequest/unauthorized/unreachable/endpoint-targeting), `ListChannelsActionTests` (4), `PostChannelActionTests` (4), `ListTemplatesActionTests` (3), `ListMarketplaceItemsActionTests` (3). Each verifies opaque-JsonElement deserialization via `GetProperty(...).GetString().ShouldBe("alpha")` against test data only the right path produces, status-code-to-failure-reason mapping via canned status responses, and exact endpoint URL via `LastRequestUri.AbsolutePath.ShouldBe("...")`. Build clean (0 warnings, 0 errors). `check-rules` clean (one stale `WorkspaceApiClient` comment in `WorkspaceLifecycleTests.cs:13` updated during the audit). `adversarial-review` clean — every new catch reachable, every typed-failure caller branches honestly, no defensive `??` on controlled contracts, tests assert against marker-substring values only the right path emits.

- **2026-05-07** (`claude/next-handoff-task-E4WGX`) — Shape C **Phase 3.5: services masquerading as static helpers, drained.** Six pre-existing static classes in `Weave.Cli.Shell` flipped to instance services with five new interfaces (`IConfigStore`, `ISecretResolver`, `IWorkspaceRegistry`, `IManifestResolver`, `ISiloLauncher`). `WorkspaceSiloStarter` deleted; its bodies fused into `SiloLauncher` along with `WorkspaceSiloPaths.ResolveSiloPath` (the only static remnant of the latter is the truly-pure `GetSiloLogPath()`). `WorkspacePrompt` flipped from static to instance (the registry/resolver-using parts); the truly-static `WriteManifestNotFound` stayed. `SiloProcessService` flipped to an instance class for the storage/auth-arg builders, with the pure-function members staying static. The two `CliConfigStore` static delegations to `CliSecretResolver` (`ResolveConnectionString`, `ToEnvReference`) dropped — call sites that need secret resolution inject `ISecretResolver` directly.

  Workspace-name completion split out: a new `WorkspaceCompletions(IWorkspaceRegistry)` instance class in `Weave.Cli.Commands` holds the one method that needed a runtime dep; the compile-time-data completions stay on the static `CliCompletions`. Each `*Command.Create()` factory grew one or two parameters (handler + `WorkspaceCompletions` / `IConfigStore` where needed) and `Program.cs` resolves them all from DI. Every `*CliCommand` handler is now in `CliServiceCollection`'s registration block.

  TUI side: `TuiShell` ctor gained `ISiloLauncher` + `IWorkspaceRegistry` + `IManifestResolver` + `PortsCliCommand`. `TuiSession` ctor gained `IManifestResolver`. `TuiWorkspaceStarter` ctor gained `ISiloLauncher`. `TuiWorkspaceDashboard` ctor gained `IWorkspaceRegistry`. `TuiSlashCommandDispatcher` ctor gained `PortsCliCommand` so the inline `new PortsCliCommand()` flipped through DI. `WorkspaceStatusCliCommand`/`WorkspaceValidateCliCommand`/`WorkspaceUpCliCommand` and the migrated read commands absorbed `IManifestResolver` + `WorkspacePrompt` ctor params; the explicit-ctor + `_field` style on those three flipped to primary ctors to match the rest of `Weave.Cli.Shell`'s style.

  Tests: 2117 → 2122 (+5). New `ManifestResolverTests` (3 — registered-name with manifest, registered-name without manifest, unknown-name) and `WorkspaceCompletionsTests` (2 — registry pass-through, empty registry). `CliSecretResolverTests` flipped from static-call form to `new CliSecretResolver()` instance form. `WorkspaceDownCliCommandTests` (5 cases) and `WorkspacePublishCommandTests` (1 ctor call) updated for the new ctor signatures via tiny `EmptyRegistry` / `NullManifestResolver` stubs co-located in the test file. `CliCommandStructureTests` updated for the new `*Command.Create(handler, …)` signatures across the migrated factories.

  Net delta: 504 insertions / 636 deletions across 91 files (the deletion bias comes from the explicit-ctor-to-primary-ctor compaction in pre-existing handlers offsetting the new interface files + DI registration block + Program.cs growth). Build clean (0 warnings, 0 errors). Full suite green: 2113 / 9 Docker-skipped. `check-rules` clean. `adversarial-review` punch list (one fix applied): WARN — initial extraction of `registry.Register(...)` from `ScaffoldWorkspaceAsync` (MarketplaceInstall) and `RestoreFilesAsync` (DataImport) up to the caller silently moved the call from "after CreateDirs, before WriteManifest" to "after the helper returns" — a real behaviour delta on partial-failure paths. Fix: passed the field-injected registry into the helper (via `private async` → `this`), restoring original ordering.

- **2026-05-07** (`claude/continue-handoff-8XcgY`) — Shape C **Phase 3: three-project split shipped.** `Weave.Cli` carved into three projects: `Weave.Cli.Shell` (shared shell primitives — theme, config, registry, version service, action-consuming `*CliCommand` handlers used by both `weave …` invocations and the TUI), `Weave.Cli.Tui` (interactive TUI shell + slash dispatcher + composer + per-verb views), and `Weave.Cli` (System.CommandLine command tree + `Program.cs` + composition root + non-shared CLI handlers). All Shell types stay `internal` with `<InternalsVisibleTo>` for `weave` (the Cli assembly name), `Weave.Cli.Tui`, and `Weave.Cli.Tests`; csproj-level `<Using Include="Weave.Cli.Shell" />` brings the shared namespace into Cli + Tui + Tests so consumer files don't repeat the using.

  ~50 files moved across the three projects (preserved via `git mv`):
  - **To `Weave.Cli.Shell`**: `CliTheme`, `VersionService` + `UpdateCache` + `UpdateCheckResult` + `VersionJsonContext`, `CliConfig` + `CliConfigStore` + `CliConfigJsonContext` + `CliSecretResolver`, `CliSystemConfigSource` + `CliSystemConfigWriter`, `WorkspaceRegistry` + `WorkspaceRegistryJsonContext`, `ManifestResolver`, `CliWorkspaceLocator`, `WorkspaceManifestFile`, `WorkspaceSiloPaths`, `WorkspaceSiloStarter`, `WorkspacePrompt`, `WorkspacePresets` + `PresetDefinition`, `CliApiHttp`, `ConsoleActionPrompter` + `ConsoleActionReporter`, `AgentsRenderer` + `ToolsRenderer` + `TasksRenderer` + `ConfigSummaryRenderer` + `SystemInfoRenderer`, `StatusMarkup` (renamed from `TuiMarkup` since it's now also used by CLI command renderers), `ICliCommand` + `NoCliOptions` + `WorkspaceNameOptions` + `WorkspaceDownOptions` + `WebUiOptions`, `WorkspaceStatusCliCommand` + `WorkspaceValidateCliCommand` + `WorkspaceDownCliCommand` + `IWorkspaceDownDependencies` + `DefaultWorkspaceDownDependencies` + `WebUiCliCommand` + `UpgradeCliCommand` + `VersionCliCommand` + `PortsCliCommand` + `WorkspacePresetsCliCommand` (the action-consuming handlers shared between `weave …` and TUI slash dispatch), and a new `WorkspaceManifestPaths` extracted from the static helpers on `WorkspaceApiClient.PrepareManifest` / `WorkspaceApiClient.GetWorkspaceStatePath`.
  - **To `Weave.Cli.Tui`**: every `Tui/*.cs` file (39 files) plus `TuiCliCommand`. `TuiApp` already lived under `Tui/`.
  - **Stayed in `Weave.Cli`**: `Program.cs`, `CliServiceCollection.cs`, every `*Command.cs` System.CommandLine factory, the unmigrated `*CliCommand` handlers (workspace new/up/run/serve/list/remove/show/publish/add-*/storage/plugin, agents/tools/tasks/system, config get/set, init, marketplace info/list/search/submit/publish/install, audit replay, data export/import), `WorkspaceApiClient` (still serves the two inline `new WorkspaceApiClient()` callsites in `DataExport`/`DataImport`), `CliCompletions` (CLI-only, System.CommandLine-coupled).

  `WorkspaceApiClient.PrepareManifest` and `WorkspaceApiClient.GetWorkspaceStatePath` extracted into a fresh `WorkspaceManifestPaths` static class in Shell, exposed as `PrepareForSilo(...)` and `GetStatePath(...)`. ~17 callsites bulk-renamed via `sed` across Cli + Shell + Tui. The seam class `WorkspaceApiClient` retains its instance methods (used by the two unmigrated Data callsites).

  Lockfiles regenerated for the new project graph; `Weave.slnx` updated to include both new projects under `/10 - UX/`. Tests stable at 2117 / 2108 passed / 9 Docker-skipped — pure organizational refactor, no behaviour change. Build clean (0 warnings, 0 errors). `check-rules` clean (no Legacy/back-compat shims, no horizontal cuts, no DateTime.UtcNow added, file/class sizes all within thresholds — largest moved file is `VersionService.cs` at 197 lines). `adversarial-review` clean (no dead branches added, no UX message conflation, no defensive null-coalesce, no test-passes-for-wrong-reason hits, no DTO chain growth, no over-engineering, no style drift).

- **2026-05-07** (`claude/continue-handoff-8XcgY`) — Shape C **Phase 2 wrap-up batch (all four remaining verbs)**: `SetConfigAction`, `OpenWorkspaceAction`, `SelectAgentAction`, `WatchWorkspaceAction` shipped. **Phase 2 is now complete.** New files in [`Weave.Actions/`](../src/UX/Weave.Actions/): `Config/{ISystemConfigWriter,SetConfigAction,SetConfigInput,SetConfigResult}.cs`; `Workspace/{IWorkspaceLocator,OpenWorkspaceAction,OpenWorkspaceInput,OpenWorkspaceResult,WatchWorkspaceAction,WatchWorkspaceInput,WatchWorkspaceResult}.cs`; `Agent/{SelectAgentAction,SelectAgentInput,SelectAgentResult}.cs`. `ConfigKeys` extended with a `Writable` subset (`siloPath`, `defaultPort`) — the unwritable members are derived (`baseUrl` from env+port, `weaveHome` from user profile, `version` from assembly) or are runtime-shaped with richer setup flows (`storage` → `weave storage change`, `authMode`/`requireHttps` → `weave init`).

  CLI/TUI bindings: `CliSystemConfigWriter` in [`Commands/Config/`](../src/UX/Weave.Cli/Commands/Config/) loads → mutates → saves via `CliConfigStore`; `CliWorkspaceLocator` in [`Commands/Workspace/`](../src/UX/Weave.Cli/Commands/Workspace/) wraps `WorkspaceRegistry.GetAll()` and `ManifestResolver.Resolve(...)`. `ConfigSetCliCommand` ctor-takes `(SetConfigAction, IActionPrompter)` — the inline `AnsiConsole.Prompt(new SelectionPrompt<string>...)` for the key picker and the `TextPrompt` for the value moved to `_prompter.PromptSelectionAsync` / `PromptTextAsync`. `TuiWorkspaceOpener` ctor-gained `OpenWorkspaceAction`; the inline registry walk + name match + cancel handling collapsed into a single action call. The original UX writer split (Warning when the registry is empty, Error when the user typed a name that doesn't match) is preserved by the TUI side, since the action layer collapses both into `NotFound`. `TuiAgentSelector` ctor-gained `SelectAgentAction`; the inline Spectre `SelectionPrompt` and case-insensitive match collapsed into a single action call. `TuiLiveStatusWatcher` and `TuiLiveStatusView` both flipped from holding three separate read actions (`GetWorkspaceStatusAction` + `ListAgentsAction` + `ListToolsAction`) to a single `WatchWorkspaceAction`; the per-tick "status required, agents/tools best-effort" composition moved into the action so the dashboard / future watchers reuse it. `ConfigSetCommand.Create` factory now takes a handler arg mirroring the existing `ConfigGetCommand.Create(handler)` pattern; `Program.cs` and `CliCommandStructureTests` updated. Orphaned `ConfigValueAccessor.cs` deleted (no callers after the migration).

  DI: `Weave.Actions.Config.ISystemConfigWriter → CliSystemConfigWriter`, `Weave.Actions.Workspace.IWorkspaceLocator → CliWorkspaceLocator` registered as singletons; `ConfigSetCliCommand` registered transient; `OpenWorkspaceAction`, `SelectAgentAction`, `WatchWorkspaceAction` registered transient inside `AddSiloActions` (no HTTP client of their own — they compose other actions or use frontend-supplied seams). `TuiShell` ctor gained `OpenWorkspaceAction` to thread through to `new TuiWorkspaceOpener(...)`.

  Tests: `Config/SetConfigActionTests.cs` (~14), `Workspace/OpenWorkspaceActionTests.cs` (~9), `Agent/SelectAgentActionTests.cs` (~9), `Workspace/WatchWorkspaceActionTests.cs` (~12). 2063 → 2108 passed (+45) + 9 Docker-skipped. Build clean (0 warnings, 0 errors). `check-rules` clean. `adversarial-review` punch list (one fix applied): WARN — initial `TuiWorkspaceOpener` collapsed both `NotFound` flavours ("registry empty" and "name not in registry") into a single `WriteWarning`, regressing the original UX which emitted Error for typed-but-unmatched names → restored the split by branching on whether `arg` was supplied. NOTEs: every `default` arm in the action's failure switch is currently unreachable (only Cancelled / NotFound fire); kept for symmetry with the locked Phase 0 contract.

- **2026-05-07** (`claude/continue-handoff-8XcgY`) — Shape C **Phase 2 run-command batch**: `weave run` migrated to `StartWorkspaceAction`. The inline `using var client = new WorkspaceApiClient($"http://localhost:{port}")` block became `using var httpClient = new HttpClient { BaseAddress = ... }` + `new StartWorkspaceAction(httpClient)`. The DI'd `StartWorkspaceAction` can't be reused here because `RunCliCommand` picks the silo port at runtime (`--port` arg) while the DI typed-client BaseAddress is fixed at `Build()` time; one-shot run-and-block lifetime makes inline `HttpClient` construction safe (no pooling concerns). Cancelled is special-cased to "Shutting down…" so Ctrl-C doesn't render as an error; the outer `catch (OperationCanceledException)` still serves the post-start `WaitForExitAsync` / `Task.Delay(Timeout.Infinite, ct)` path. The big `catch (Exception ex) when (ex is HttpRequestException or ...)` exception-bag at the bottom of the original `try` block dropped — the action returns typed failure now; the only remaining catch is for cancellation. File shrank from ~180 to ~115 lines.

  **Inline `new WorkspaceApiClient()` count: 3 → 2** (`Commands/Data/{DataExport,DataImport}CliCommand.cs` remain; both fan out across skills/channels/templates/marketplace endpoints — fully draining them needs new action verbs in those domains, deferred). Tests stable at 2063 + 9 Docker-skipped — `RunCliCommand` has no unit tests today; the migration is a pure refactor verified by build + the existing Cli.Tests `CliCommandStructureTests` exercising the command tree wiring. Build clean (0 warnings, 0 errors); full suite green. `check-rules` clean. `adversarial-review` clean — Cancelled handling is honest; the outer catch is real (Run blocks on `WaitForExitAsync`/`Task.Delay` after start succeeds); no defensive `??` on private contracts; no UX message conflation (Cancelled → "Shutting down…", others → "Failed to start workspace: …" umbrella which works because every reason is genuinely a failed start).

- **2026-05-07** (`claude/continue-handoff-8XcgY`) — Shape C **Phase 2 chat/agent-list batch**: `SendMessageAction` shipped; the inline TUI chat path (`TuiChatSession.SendAsync`) consumes it. `TuiAgentNameSource` rewritten to consume the existing `ListAgentsAction` instead of `WorkspaceApiClient.GetAgentsAsync` + `IsReachableAsync`. Two more inline `new WorkspaceApiClient()` callsites drained (count: 5 → 3). New files in [`Weave.Actions/Agent/`](../src/UX/Weave.Actions/Agent/): `SendMessageAction.cs`, `SendMessageInput.cs` (workspace + agent + content), `SendMessageResult.cs` (curated `Content` / `ConversationId` / `UsedTools` / `Model` / `Messages`), `ConversationMessage.cs` (curated history-entry record). `AgentJsonContext` extended with `SendMessageWire`, `ChatResponseWire`, `ConversationMessageWire`, `AgentProblemWire` (RFC 7807 subset for 400/409). Failure mapping per the locked verb template: 400 → `ValidationFailed` joining the silo's per-key errors; 409 → `Conflict` carrying the silo's `detail` (e.g. agent paused); 401/403 → `Unauthorized`; 5xx → `Internal`; `HttpRequestException` → `SiloUnreachable`; `OperationCanceledException` → `Cancelled`. `services.AddHttpClient<SendMessageAction>` added to `AddSiloActions`.

  CLI/TUI side: `TuiChatSession` flipped from parameterless ctor to `(SendMessageAction)`; the inline `using var client = new WorkspaceApiClient(); ...SendAgentMessageAsync...` collapsed into one `_sendMessageAction.ExecuteAsync` call inside the existing Spectre `Status().StartAsync(...)` spinner. Cancelled (Ctrl-C during a chat send) is filtered out of error rendering — the original behaviour printed "Agent call failed: …" on `TaskCanceledException` too, but mapping cancellation to a typed `ActionFailureReason.Cancelled` lets us suppress the noise cleanly. The `ApiConversationMessage` history list is now `List<ConversationMessage>` (the action layer's curated record) — the legacy CLI `ApiConversationMessage` / `ApiChatResponse` / `ApiSendMessageRequest` records still exist for the unmigrated `WorkspaceApiClient.SendAgentMessageAsync` signature on the seam class but no longer flow into TUI history. `TuiAgentNameSource` ctor-takes `ListAgentsAction`; the live-vs-manifest fallback flow is unchanged (silo unreachable or non-Cancelled non-success → manifest). DI: `TuiAgentNameSource`, `TuiAgentSelector`, `TuiChatSession` registered transient in `CliServiceCollection`; `TuiShell` ctor injects all three (the inline `new TuiAgentNameSource()` / `new TuiAgentSelector(...)` and the `private readonly TuiChatSession _chatSession = new();` field initializer all drained). `TuiAgentSelector` itself stays unchanged behaviorally; it's now resolvable through DI.

  Tests: `Agent/SendMessageActionTests.cs` (15 — 2 are blank-id `[Theory]` rows on a 4-row matrix counted as 1 method) covering 200-success + history translation, 400-validation with extracted-key-list and fallback-default, 409-conflict with the `<<MARKER-FROM-SILO>>` token discipline + the no-detail fallback, 401-Unauthorized, 500-Internal, `HttpRequestException → SiloUnreachable`, escaped-segment endpoint-targeting (workspace + agent paths), explicit `HttpMethod.Post` assertion, null-input, blank-identifier `[Theory]`. Tests 2048 → 2063 (+15) + 9 Docker-skipped. Build clean (0 warnings, 0 errors); full suite green. `check-rules` clean apart from the pre-existing `TuiSlashCommandDispatcher.cs` 212-line trajectory NOTE (no change to that file in this batch). `adversarial-review` punch list: WARN — initial `SendMessageAction` had a `NotFound` arm dead under current contract (silo doesn't 404 on missing agent today; the actor system creates one) → arm dropped along with its test, mirrors the same fix from the Stop arm in the prior batch; NOTE — `TuiChatSession`'s "Agent call failed: …" template would render `Cancelled` reason as awkward error noise → early-return on `Cancelled` so Ctrl-C is silent.

- **2026-05-07** (`claude/continue-handoff-8XcgY`) — Shape C **Phase 2 start/stop batch**: `weave up` / `weave down` and TUI `/up` / `/down` migrated to `StartWorkspaceAction` / `StopWorkspaceAction`. New files in [`Weave.Actions/Workspace/`](../src/UX/Weave.Actions/Workspace/): `StartWorkspaceAction.cs`, `StartWorkspaceInput.cs` (typed `WorkspaceManifest` input — frontend reads + parses + path-resolves before calling), `StartWorkspaceResult.cs` (reuses `WorkspaceStatusSummary` since silo emits the same `WorkspaceResponse` shape on 201 as on 200), `StopWorkspaceAction.cs`, `StopWorkspaceInput.cs`, `StopWorkspaceResult.cs` (no-payload sentinel for `ActionResult<>` symmetry). `WorkspaceJsonContext` extended with `StartWorkspaceWire { WorkspaceManifest Manifest }` request envelope, `WorkspaceProblemWire { Errors, Detail, Title }` (RFC 7807 subset for 400/409 problem-detail extraction), and `WorkspaceManifest` registered for source-gen (camelCase + `WhenWritingNull` + `UseStringEnumConverter` to match the silo's API binder). Failure mapping per the locked verb template: 400 → `ValidationFailed` with the silo's per-key errors map joined as `"{key}: {msg}"`; 409 → `Conflict` carrying the silo's RFC 7807 `detail` (with fallback when missing); 401/403 → `Unauthorized`; 5xx → `Internal`; `HttpRequestException` → `SiloUnreachable`; `OperationCanceledException` → `Cancelled`. `Weave.Actions.csproj` ProjectReference to `Weave.Workspaces` added back (validate's earlier removal was case-specific — it sends manifest text as a JSON string; start sends the typed object so STJ source-gen needs the type). DI: `services.AddHttpClient<StartWorkspaceAction>` + `<StopWorkspaceAction>` lines in `AddSiloActions`.

  CLI/TUI side: `WorkspaceUpCliCommand` reshaped to take `StartWorkspaceAction` + `GetSystemInfoAction` via ctor; the inline `using var client = new WorkspaceApiClient(); ...IsReachableAsync... StartWorkspaceAsync...` block became one `_systemInfoAction.ExecuteAsync` reachability probe + one `_startAction.ExecuteAsync` POST. The auto-start-silo flow stays put (frontend orchestration, separate from the silo HTTP). `TuiWorkspaceStarter` flipped from `(agentSelector)` to `(agentSelector, StartWorkspaceAction, GetSystemInfoAction)`; its `ApiWorkspaceResponse?` + `Exception?` outcome variables collapsed into one `ActionResult<StartWorkspaceResult>`. `WorkspaceDownCliCommand` keeps the existing `IWorkspaceDownDependencies` testability seam; `DefaultWorkspaceDownDependencies` now ctor-takes `StopWorkspaceAction`. The original Phase 2 PR translated `ActionFailure` back into `InvalidOperationException` to keep the CLI command's catch shape; the `2026-05-08` follow-up flipped `IWorkspaceDownDependencies.StopWorkspaceAsync` to return `Task<ActionResult<StopWorkspaceResult>>` directly so the command switches on the typed result and the catch list shrinks to `IOException`/`UnauthorizedAccessException` (the local `DeleteFile` cleanup). See the matching History entry. `TuiWorkspaceStopper` flipped from `internal static` to instance class taking `WorkspaceDownCliCommand`; `TuiSlashCommandDispatcher` ctor + `_workspaceStopper` field gained that param; `TuiShell` threads `WorkspaceDownCliCommand` from DI down through. `WorkspaceUpCommand`/`WorkspaceDownCommand` factories now take handler args mirroring the existing `WorkspaceStatusCommand.Create(handler)` pattern; `Program.cs` and `CliCommandStructureTests` updated to pass DI-resolved instances. `CliServiceCollection` registers `WorkspaceUpCliCommand`, `WorkspaceDownCliCommand`, and `IWorkspaceDownDependencies → DefaultWorkspaceDownDependencies` as transients. The static helpers `WorkspaceApiClient.PrepareManifest` and `WorkspaceApiClient.GetWorkspaceStatePath` stay where they are — both are frontend orchestration (path resolution, on-disk state path) that the action layer doesn't model.

  **Inline `new WorkspaceApiClient()` count: 8 → 5** (`WorkspaceUpCliCommand`, `TuiWorkspaceStarter`, `DefaultWorkspaceDownDependencies` drained). Remaining: `TuiAgentNameSource`, `TuiChatSession`, `Commands/Workspace/RunCliCommand.cs`, `Commands/Data/{DataExport,DataImport}CliCommand.cs` — all on Phase 2 write paths still to migrate.

  Tests: `Workspace/StartWorkspaceActionTests.cs` (10) covers 201-success + translation, 400-validation with the per-key errors-map join + the no-errors fallback default, 409-conflict with the `<<MARKER-FROM-SILO>>` token discipline that ensures the assertion can't pass for the wrong reason + the no-detail fallback, 401-Unauthorized, 500-Internal, `HttpRequestException → SiloUnreachable`, endpoint-targeting (`/api/workspaces`), null-input. `Workspace/StopWorkspaceActionTests.cs` (11) covers 204-success, 409-conflict-with-detail + no-detail-fallback, 401-Unauthorized, 500-Internal, `HttpRequestException → SiloUnreachable`, escaped-id-endpoint-targeting, an explicit `HttpMethod.Delete` assertion, null-input, and a blank-id `[Theory]` (2 rows). Tests 2027 → 2048 (+21) + 9 Docker-skipped. Build clean (0 warnings, 0 errors); full suite green. `check-rules` clean apart from the `TuiSlashCommandDispatcher.cs` 212-line trajectory NOTE (was 209, +3 lines from the stopper field/param/case body — Phase 4 plans to drain via a generic `/verb` → action adapter). `adversarial-review` punch list: WARN — `StopWorkspaceAction` had a `NotFound` arm dead under current contract (`WorkspaceActor.StopAsync` no-ops when not Running, so the silo never 404s on stop) → arm dropped along with its test; WARN — `StartWorkspaceActionTests.ExecuteAsync_409_ReturnsConflictWithSiloDetail` initial assertion `ShouldContain("already running")` matched both the stub detail and the action's fallback default → switched to a `<<MARKER-FROM-SILO>>` substring only present in the stub. NOTEs: `WorkspaceUpCliCommand` / `TuiWorkspaceStarter` use a single "Failed to start" template across all `Failure.Reason` values — works because every reason is genuinely a failed start, but could differentiate Conflict → warning vs. SiloUnreachable → warning + retry hint for sharper UX (deferred); `HttpStatusCode.OK` arm in `StartWorkspaceAction`'s success case is defensive (silo emits 201, never 200) but harmless.

- **2026-05-07** (`claude/continue-handoff-plan-Ob3eG`) — `/system` TUI migrated; **Phase 1 reads now fully shipped, including all TUI consumers**. `TuiSystemView` flipped from `internal static class` to `internal sealed class TuiSystemView(GetSystemInfoAction)`; the inline `using var client = new WorkspaceApiClient()` pair (`TuiSystemView` + `TuiRuntimeProbe`) drained. `TuiRuntimeProbe.cs` deleted entirely — its only other consumer (`TuiWorkspaceDashboard`) also took `GetSystemInfoAction` via ctor since the probe was a one-line wrapper around the same `/health` GET that the action already does. `Commands/System/SystemInfoRenderer.cs` extracted as `internal static`, deduping the row-by-row table layout that had drifted between `SystemCliCommand.Render` and the old `TuiSystemView.ShowAsync` (mirrors `AgentsRenderer`/`ToolsRenderer`/`TasksRenderer`/`ConfigSummaryRenderer` — five shared renderers now, one per Phase 1 read with table output). DI plumbing: `TuiWorkspaceDashboard` and `TuiSystemView` registered transient in `CliServiceCollection`; `TuiShell` injects both (the inline `new TuiWorkspaceDashboard()` in `TuiShell` drained); `TuiSlashCommandDispatcher` gains a `TuiSystemView` field/ctor param and the `case "system"` arm calls the instance.

  Behavior tweak (intentional): the TUI's "Silo API" row now renders `info.BaseUrl` (`WEAVE_API_URL` env override or `http://localhost:{WeavePorts.SiloHttp}`) instead of the old `http://localhost:{config.DefaultPort}`. This aligns the TUI with what `weave system` already showed and respects the env override that the silo probe itself uses; the previous TUI rendering was a drift from the action layer's source of truth. Tests stable at 2036 / 2027 passed / 9 Docker-skipped — pure refactor, no new tests. `check-rules` clean except the `TuiSlashCommandDispatcher.cs` 209-line trajectory NOTE (Phase 4 plans to drain via a generic `/verb` → action adapter; +2 lines this diff). `adversarial-review` clean except a NOTE on the unreachable `if (!result.IsSuccess)` arm in both `TuiSystemView` and `TuiWorkspaceDashboard` — same trade-off as `WebUiCliCommand`'s unreachable failure branch and same locked Phase 0 contract symmetry (every action returns `Task<ActionResult<T>>`; `[MemberNotNullWhen(true, nameof(Value))]` on `ActionResult<T>` makes the IsSuccess guard a type-system requirement). Inline `new WorkspaceApiClient()` count drops from 10 → 8 with this commit (the eight remaining are all on Phase 2 write paths).

- **2026-05-07** (`claude/continue-handoff-plan-Ob3eG`) — Shape C Phase 1, the last two read-only verbs: `weave config get` and `weave webui` migrated, completing Phase 1 reads (7/7 planned verbs in flight). `GetConfigAction` is the first local-only Phase 1 action — no silo round-trip because `~/.weave/config.json` is purely client-side; takes `ISystemConfigSource` only and renders through a curated `ConfigSummary` record with all formatting baked in (`int → decimal`, `bool → "true"/"false"`, `string? SiloPath → "(not set)"`). Single-key path returns `ActionFailure.ValidationFailed` with the full valid-keys list when the key is unknown; whole-snapshot path renders all eight fields through the shared `ConfigSummaryRenderer`, consolidating the previous CLI-3-row vs TUI-5-row drift. `SystemConfigSnapshot` gained a `Version` field so the config-format version stays surfaced via the action layer. The orphaned `ConfigValueAccessor.GetValue` helper (no caller after migration; `SetValue` retained) was deleted in the same commit per the no-back-compat rule. `GetDashboardStatusAction` resolves the dashboard URL (caller-supplied or computed from `ISystemConfigSource` using the standard `WeavePorts` silo-vs-dashboard pairing) and probes its reachability via the typed `HttpClient`; preserves the existing `IsSuccessStatusCode || statusCode < 500` semantic so 4xx (auth/not-found) still maps to reachable=true. The action's typed `HttpClient` registers under the same silo `BaseAddress` but the action passes absolute URIs to `GetAsync` so the BaseAddress is unused — handler pooling consistency at zero cost. Browser-launching stays in `WebUiCliCommand` (OS-specific side effect, not action contract). The CLI command reads `WEAVE_WEBUI_URL` itself before calling the action so the action stays env-var-agnostic. `WebUiCliCommand` migrated to constructor-injected DI via `WebUiCliCommand(GetDashboardStatusAction)`; the inline `new WebUiCliCommand()` in `TuiSlashCommandDispatcher` and the static `WebUiCommand.Create()` both updated to resolve through DI. `TuiConfigView` flipped from static to instance-class `internal sealed class TuiConfigView(GetConfigAction)`, registered transient.

  Tests: `Config/GetConfigActionTests.cs` (16: 10-row case-insensitive Theory across all 8 keys + unknown-key validation + snapshot rendering + cancelled-token + null-input); `Dashboard/GetDashboardStatusActionTests.cs` (11: default-port URL computation, non-default port (`DefaultPort+1`), explicit URL, whitespace-fallthrough, 5-row status-code Theory across the reachability boundary, HttpRequestException → reachable=false, null-input). Tests 2000 → 2027 (+27) + 9 Docker-skipped. Build clean (0 warnings, 0 errors). `check-rules` clean apart from the `TuiSlashCommandDispatcher.cs` 200-line trajectory NOTE (Phase 4 plans to drain via a generic `/verb` → action adapter; not a new concern). `adversarial-review` flagged one orphan (`ConfigValueAccessor.GetValue`) which was removed during review, plus a NOTE that `WebUiCliCommand`'s `if (!result.IsSuccess)` branch is unreachable today — kept for contract symmetry across the seven Phase 1 verbs since the locked Phase 0 decision says every action returns `Task<ActionResult<T>>` and frontends check `IsSuccess` uniformly.

- **2026-05-07** (`claude/continue-handoff-plan-Ob3eG`) — `adversarial-review` skill enforcement wired: a `PreToolUse` hook (`.claude/hooks/require-adversarial-review.sh`) blocks `git commit` until the staged-diff fingerprint in `.claude/.adversarial-review-marker` (gitignored) matches `git diff --cached | sha1sum`. The marker is written by the agent after running the skill; restaging invalidates it (any change after the review pass forces a re-review). Override path for legitimate non-`src/` commits (lockfile regen, infra-only changes, typo fixes): `WEAVE_SKIP_ADVERSARIAL_REVIEW=1 git commit ...`. Hook registered in `.claude/settings.json` under `PreToolUse` → `Bash` matcher. Six test scenarios pass: non-`git commit` no-op, nothing-staged no-op, override env-var no-op, staged-diff-no-marker block, valid-marker pass-through, stale-marker-after-restage block. The `check-rules` skill catches what `grep` can find; `adversarial-review` catches what it can't (dead branches, conflated UX messages, defensive nulls on contracts you control, tests that pass for the wrong reason, redundant DTO chains, style drift, over-engineering for private contracts) — both must come back clean before commit.

- **2026-05-06** (`claude/continue-handoff-plan-Ob3eG`) — Adversarial review of the validate-via-silo work; cleaned up nine real smells. **BLOCK fixes:** dropped the unreachable `catch (FormatException)` in `WorkspaceEndpoints.ValidateManifestAsync` (`ManifestParser.Parse` only throws `JsonException`); fixed the CLI message conflation in `WorkspaceValidateCliCommand` so `SiloUnreachable` no longer renders as `"Configuration invalid: …"` (now switches on `Failure.Reason` and routes ValidationFailed → `WriteError "Configuration invalid"`, SiloUnreachable → `WriteWarning`, NotFound → `WriteError`). **WARN fixes:** simplified `ExtractProblemMessage` to a one-liner that only reads the `errors.manifestJson[0]` key the silo actually emits (the title/detail/multi-key fallback chain was over-engineering for a private CLI/silo contract); added explicit branching for `Unauthorized`/`Forbidden`/5xx HTTP statuses (previously all collapsed into `SiloUnreachable` via `EnsureSuccessStatusCode`); strengthened the 400 test to assert against a marker string only present in the stub (`<<MARKER-FROM-SILO>>`) so it actually exercises the extraction path rather than coincidentally matching the fallback default. **NOTE fixes:** dropped `ValidateWorkspaceManifestResponse` + its `FromResult` mapper — the endpoint now returns the CQRS result type directly, registered in `SiloApiJsonContext`; aligned wire-record style (all property-init, matching `WorkspaceWire`/`AgentWire`/etc.); tightened `ValidateWorkspaceResultWire.Errors` to non-nullable `List<string>` with default `[]`, dropped the `?? []` in the action; collapsed two try/catch blocks into one — the file read and HTTP call now share a single `OperationCanceledException` arm.

  Tests: 3 new ValidateWorkspaceAction tests (`SiloReturns400_WithoutManifestJsonKey_FallsBackToDefault`, `SiloReturns401_MapsToUnauthorized`, `SiloReturns500_MapsToInternal`). 1997 → 2000 (+3) + 9 Docker-skipped. Build clean (0 warnings, 0 errors); `check-rules` clean.

- **2026-05-06** (`claude/continue-handoff-plan-Ob3eG`) — Validate moved silo-side: `ValidateWorkspaceAction` was reshaped from a CLI-local parse+validate (which duplicated logic the silo would eventually need anyway) into a thin HTTP wrapper around a new silo endpoint. Reasoning: actions are frontend orchestration, not business logic — the silo is the brain. New silo-side pieces: `ValidateWorkspaceManifestQuery(ManifestJson) → ValidateWorkspaceManifestResult` + handler in `src/Workspaces/Weave.Workspaces/Manifest/`; `IManifestParser` registered silo-side in `SiloServiceRegistrar.RegisterKernel()` (singleton); new endpoint `POST /api/workspaces/validate` in `WorkspaceEndpoints.cs` that takes a `ValidateWorkspaceManifestRequest { ManifestJson }` body, dispatches the CQRS query, and returns a `ValidateWorkspaceManifestResponse` (200 OK with `errors[]` for structural-invalid; 400 ProblemDetails for parse failure / blank body). Action side: `ValidateWorkspaceAction` now takes `HttpClient`, reads the manifest from disk (frontend orchestration), POSTs the JSON(C) text, and translates 200/400 responses into `ActionResult`. New `ValidateWorkspaceJsonContext.cs` carries the per-feature wire records (`ValidateWorkspaceWire`, `ValidateWorkspaceResultWire`, plus a minimal `ProblemWire` to extract the parse-error message from RFC 7807). Registered as `AddHttpClient<ValidateWorkspaceAction>` like every other HTTP-bound action; the prior `AddTransient` + `IManifestParser` Singleton registration is gone. `Weave.Actions.csproj` ProjectReference to `Weave.Workspaces` removed (action no longer needs the parser locally); lockfile regenerated.

  Tests: 4 new silo endpoint tests in `WorkspaceEndpointBranchTests` (valid manifest → 200 with `errors[]==[]`, structurally-invalid → 200 with `errors.length>0`, invalid JSON → 400, blank body → 400) and the existing `ValidateWorkspaceActionTests` rewritten to drive HTTP via `StubHttpMessageHandler` (NSubstitute's `IManifestParser` stub gone — no longer needed since the action doesn't talk to the parser). 1991 → 1997 (+6: 4 silo + 2 net Actions).

- **2026-05-06** (`claude/continue-handoff-plan-Ob3eG`) — Shape C Phase 1, status + validate batch: `GetWorkspaceStatusAction` (HTTP) and `ValidateWorkspaceAction` (local, no HTTP) shipped; `weave workspace status` / `weave workspace validate` CLI commands migrated to consume them; `TuiLiveStatusView` + `TuiLiveStatusWatcher` (the `/watch` slash and the auto-render in `TuiWorkspaceOpener`) drained off `WorkspaceApiClient` and now compose `GetWorkspaceStatusAction` + `ListAgentsAction` + `ListToolsAction` directly. New folder `Weave.Actions/Workspace/` carries `GetWorkspaceStatusAction.cs`, `WorkspaceStatusSummary`, the input/result records, and `WorkspaceJsonContext` (per-feature source-gen JSON context co-located with a private `WorkspaceWire`). The action handles 404 explicitly → `ActionFailureReason.NotFound`; non-2xx other than 404 falls through `EnsureSuccessStatusCode` → `HttpRequestException` → `SiloUnreachable`. `ValidateWorkspaceAction` is the first local-only action: takes `IManifestParser` via ctor (CA1822 forced the dep, which improves testability), reads the manifest from disk, and surfaces parse errors as `ActionFailure.ValidationFailed` while structural errors ride inside the result so the frontend can render the per-error list. Registered as `AddTransient<>` rather than `AddHttpClient<>` since there's no typed client; `IManifestParser` registered as `TryAddSingleton`. `Weave.Actions.csproj` gained a `ProjectReference` to `Weave.Workspaces` for the parser.

  CLI side: `WorkspaceStatusCliCommand` now takes `GetWorkspaceStatusAction` + `ListAgentsAction` + `ListToolsAction` via ctor and composes all three for the live view (manifest fallback unchanged); `WorkspaceValidateCliCommand` takes `ValidateWorkspaceAction`. `WorkspaceStatusCommand.Create` and `WorkspaceValidateCommand.Create` gained a handler parameter — `Program.cs` and `CliCommandStructureTests` updated to pass DI-resolved instances. `TuiSlashCommandDispatcher` no longer instantiates the CLI commands inline (`new WorkspaceStatusCliCommand()` / `new WorkspaceValidateCliCommand()`) — both flow through the dispatcher's ctor. `TuiLiveStatusRenderer` retyped its parameters from the wire DTOs (`ApiWorkspaceResponse`/`ApiAgentResponse`/`ApiToolResponse`) to the curated action shapes (`WorkspaceStatusSummary`/`AgentSummary`/`ToolSummary`) — pure parameter-type swap, render layout unchanged.

  Tests: `Workspace/GetWorkspaceStatusActionTests.cs` (8) and `Workspace/ValidateWorkspaceActionTests.cs` (7) — the latter mixes real `ManifestParser` against on-disk JSONC for happy/invalid-JSON paths with an `NSubstitute`-stubbed `IManifestParser` for the "structurally invalid" branch (cleanest way to drive `Validate(...) → [errors]` without crafting a manifest the real parser deems structurally invalid). Tests 1976 → 1991 (+15) + 9 Docker-skipped. Build clean (0 warnings, 0 errors); `check-rules` clean apart from a NOTE on `DateTime.Now` in `TuiLiveStatusRenderer`'s "Refreshed at" label — pre-existing, unchanged by this refactor, and explicitly allowed (display timestamp, not behaviour). `Weave.Actions/packages.lock.json` regenerated to reflect the new `Weave.Workspaces` ProjectReference.

- **2026-05-06** (`claude/continue-handoff-plan-Ob3eG`) — Shape C Phase 1, two more verbs: `weave tools` and `weave tasks` shipped, with the two named TUI views (`TuiToolsView`, `TuiTasksView`) migrated off `WorkspaceApiClient` to consume `ListToolsAction` / `ListTasksAction` directly. Two new feature folders in `Weave.Actions`: `Tool/{ToolSummary,ListToolsInput,ListToolsResult,ListToolsAction,ToolJsonContext}.cs` calling `GET /api/workspaces/{Uri.EscapeDataString(id)}/tools`, and `AgentTask/{TaskSummary,ListTasksInput,ListTasksResult,ListTasksAction,TaskJsonContext}.cs` calling `GET /api/workspaces/{Uri.EscapeDataString(id)}/agents/{Uri.EscapeDataString(agentName)}/tasks`. Each verb follows the locked `ListAgentsAction` template: typed `HttpClient`, private `internal sealed record XxxWire` co-located with the per-feature source-gen `JsonSerializerContext`, ~10-line Wire→Summary translation loop, three failure paths (`OperationCanceledException` → `Cancelled`, `HttpRequestException` → `SiloUnreachable`, blank/null guards via `ArgumentException.ThrowIfNullOrWhiteSpace`/`ArgumentNullException.ThrowIfNull`). Folder is `AgentTask/` not `Task/` — `Weave.Actions.Task` namespace would shadow `System.Threading.Tasks.Task` for any file inside, same gotcha as `SystemInfo/` vs `System/` from Phase 0; pre-emptively renamed to avoid scattered `global::` qualifiers. `SiloActionsServiceCollectionExtensions` gained two more `AddHttpClient<>` lines.

  CLI side: `Commands/Tools/{ToolsCliCommand,ToolsCommand,ToolsRenderer}.cs` wires `weave tools [workspace]`; `Commands/Tasks/{TasksOptions,TasksCliCommand,TasksCommand,TasksRenderer}.cs` wires `weave tasks [workspace] [agent]`. Tools mirrors `weave agents` shape — guided/advanced modes, manifest-only fallback when silo unreachable. Tasks is silo-only (tasks are an agent-runtime concept, not declared in the manifest), with a 2-arg shape and an interactive agent picker that auto-selects the only agent if the manifest declares one. `TuiToolsView` and `TuiTasksView` ctor-gained their action class and dropped their `WorkspaceApiClient` dep; both now bottom out at the shared `ToolsRenderer` / `TasksRenderer` instead of inline Spectre table-building. The `services.AddSingleton<WorkspaceApiClient>()` registration in `CliServiceCollection` was dropped along with the migration (no DI consumer remained — the 12 inline `new WorkspaceApiClient()` callsites enumerated in the Hygiene queue all instantiate locally; static helpers `GetWorkspaceStatePath`/`PrepareManifest` keep working without the registration).

  Tests: `Tool/ListToolsActionTests.cs` (7) and `AgentTask/ListTasksActionTests.cs` (10) — both use `StubHttpMessageHandler.Returns/Throws` to drive the action without hitting the network, covering wire-JSON-translation, empty-array, `HttpRequestException → SiloUnreachable`, escaped-id-endpoint-targeting, null-input, and a blank-identifier `[Theory]` (Tasks adds 4 rows for the workspace+agent matrix). Tests 1958 → 1976 (+18; the +1 over the +17 new tests is unrelated — fixed in the same commit, see below). Build clean (0 warnings, 0 errors); full suite green; `check-rules` clean apart from a NOTE on XML doc summaries that mirror the Phase 0 template.

  Pre-existing build break fixed in the same commit: `PostgreSqlBuilder()` parameterless ctor was deprecated in the Testcontainers 4.7 → 4.11 dependabot bump (PR #54) which was merged after Phase 0 shipped, leaving `src/Security/Weave.Security.Tests/PostgresContainerFixture.cs` blocking `dotnet build Weave.slnx` (warnings-are-errors). Replaced `new PostgreSqlBuilder().WithImage(ImageTag).Build()` with `new PostgreSqlBuilder(ImageTag).Build()` per the obsoletion message — minimal fix, no behaviour change.

- **2026-05-05** (`claude/ui-revamp-awdvG`) — Shape C Phase 0 + Phase 1 first verb shipped (`weave system` + `weave agents`); the action layer is now actions-own-HTTP and the foundation for the remaining six Phase 1 verbs is locked. New `Weave.Actions` project at `src/UX/Weave.Actions/` (added to `Weave.slnx` under `/10 - UX/`) carries: `Context/{IActionPrompter,IActionReporter,ActionResult,ActionFailure,ActionFailureReason}.cs` (frontend prompter/reporter, typed-result `readonly record struct` with `[MemberNotNullWhen]` and a non-generic `ActionResult` factory companion to satisfy CA1000, `ActionFailure` record carrying `ActionFailureReason` enum: `SiloUnreachable`/`ValidationFailed`/`NotFound`/`Conflict`/`Unauthorized`/`Cancelled`/`Internal`/`Unknown` with per-reason factories), `SiloActionsServiceCollectionExtensions.cs` (`AddSiloActions(configureClient)` — registers a typed `HttpClient` per action via `AddHttpClient<TAction>` so each action gets its own client through `IHttpClientFactory`), `SystemInfo/{ISystemConfigSource,SystemConfigSnapshot,GetSystemInfoInput,SystemInfoResult,GetSystemInfoAction}.cs` (the pilot — combines a frontend-supplied snapshot with a typed-`HttpClient` `GET /health` probe under a 2-second linked timeout), and `Agent/{AgentSummary,ListAgentsInput,ListAgentsResult,ListAgentsAction,AgentJsonContext}.cs` (first Phase 1 verb — calls `GET /api/workspaces/{Uri.EscapeDataString(id)}/agents` via typed `HttpClient`, deserializes into a private `internal sealed record AgentWire` defined alongside the per-feature source-gen `JsonSerializerContext`, maps to the curated public `AgentSummary`). Folder is `SystemInfo/` not `System/` — the latter shadows `global::System.*` for any file in the namespace and was renamed up front to avoid scattered `global::` qualifiers.

  The `IActionLogger` slot from the original Shape C plan is **deferred** — actions that need logging take `ILogger<TAction>` from `Microsoft.Extensions.Logging.Abstractions` directly (already the test-guidance default; no bespoke wrapper). Mid-session a conservative-path attempt was tried (an `ISiloProbe` placeholder grew into `ISiloApi` with a `ListAgentsAsync` method, `WorkspaceApiClient` translated `ApiAgentResponse → AgentSummary` at the boundary) and **rolled back in the same commit** when the trade-off became visible: each remaining Phase 1 verb would add a translation slice, and the alternative (promoting the existing `internal sealed` CLI silo client + 11 wire DTOs to `public`) locks in a public API surface that's actually a private CLI/silo HTTP contract. Final shape: actions own their HTTP, no shared client abstraction, ~10-line wire-record + per-feature `JsonSerializerContext` per verb.

  CLI side: new `ActionContext/ConsoleActionPrompter.cs` + `ConsoleActionReporter.cs` (Spectre + `CliTheme` adapters); `Commands/System/{CliSystemConfigSource,SystemCliCommand,SystemCommand}.cs` wires `weave system`; `Commands/Agents/{AgentsCliCommand,AgentsCommand,AgentsRenderer}.cs` wires `weave agents [workspace]` (guided + advanced modes; manifest-only fallback when silo unreachable). `AgentsRenderer` is the shared Spectre-table layout consumed by both the CLI command and the migrated TUI `TuiAgentListView` — `TuiShell` ctor gained `ListAgentsAction` to thread it in; `TuiAgentListView` swapped its inline `using var client = new WorkspaceApiClient()` for the action. `WorkspaceApiClient` stayed plain `internal sealed class WorkspaceApiClient : IDisposable` in `Weave.Cli.Commands` — no `Weave.Actions` interface, no translation method — it serves the remaining 12 unmigrated callsites (was 13 before this session) and deletes itself when the last one migrates.

  All seven Cli↔Actions wirings registered in `CliServiceCollection.Build()`: `IActionPrompter`/`IActionReporter`/`ISystemConfigSource` as singletons, `services.AddSiloActions(c => c.BaseAddress = new Uri(CliApiHttp.ResolveBaseUrl()))` as the one-line action+typed-client registration, `SystemCliCommand`/`AgentsCliCommand` as transients. `weave system` and `weave agents` both wired in `Program.cs` (between `MarketplaceCommands` and `VersionCommand`). `Microsoft.Extensions.Http` 10.0.7 added to `Directory.Packages.props` for the typed-client pattern.

  Tests: new `Weave.Actions.Tests` project (added to `Weave.slnx`) with 16 unit tests — `Context/ActionResultTests.cs` (4 covering Success/Failed shape + factory reasons + Cancelled default), `SystemInfo/GetSystemInfoActionTests.cs` (5 driving the action against a `StubHttpMessageHandler` covering health-200 / health-500 / `HttpRequestException`-throws-during-probe / `/health`-endpoint-targeting / null-input), `Agent/ListAgentsActionTests.cs` (7 covering wire-JSON-translation / empty-array / `HttpRequestException → SiloUnreachable` / escaped-workspace-id-endpoint-targeting / null-input / blank-workspace-id theory). The shared `Helpers/StubHttpMessageHandler.cs` is the reusable test seam (`Returns(status, body)` and `Throws(ex)` factories plus a `LastRequestUri` capture).

  Build clean (0 warnings, 0 errors); full suite 1942 → 1958 (+16) + 9 Docker-skipped. Phase 1 verbs 2–7 (`ListToolsAction`, `ListTasksAction`, `GetWorkspaceStatusAction`, `GetConfigAction`, `DashboardAction`, `ValidateWorkspaceAction`) follow the locked template — see "How Shape C is wired today" for the per-verb shape.

- **2026-05-05** (`claude/review-handoff-task-nBUNT`, [PR #62](https://github.com/KoalaFacts/Weave/pull/62)) — TUI hidden-dep fix shipped (the original handoff item) + Shape C migration plan recorded as next-work top priority. The two named views (`TuiToolsView`, `TuiTasksView`) are now `internal sealed class TuiXxxView(WorkspaceApiClient client)` instead of `internal static class` with `using var client = new WorkspaceApiClient();` inline; both registered Transient in `CliServiceCollection`, threaded through `TuiShell` → `TuiSlashCommandDispatcher`. `WorkspaceApiClient` is registered Singleton (Microsoft's HttpClient guidance is one-per-application, not one-per-call). The other 13 inline `new WorkspaceApiClient()` callsites stay — they get drained as part of the Shape C migration. The session also explored a much larger TUI refactor (one-interface + keyed-DI dispatcher + 23 command classes + 10 helper interfaces) across five exploratory commits; a step-back review concluded the structure was already wrong (TUI duplicates CLI command surface, helpers couple Spectre prompts inline, no path for non-Spectre frontends). All exploratory commits force-rolled-back; only the original two-file fix retained. Tests stable at 1942 + 9 Docker-skipped.
- **2026-05-05** (`claude/job-handoff-continuation-cYEva`) — direct state-method test coverage for the Tell-don't-ask refactor from PR #58. Two new test files in `src/Assistants/Weave.Agents.Tests/`: `SkillMemoryStateTests.cs` (16 tests) and `EpisodicMemoryStateTests.cs` (14 tests), 30 total. They drive `SkillMemoryState.Search` and `EpisodicMemoryState.Recall` directly — no actor, no `CapabilityAuthorizer`, no `IEventBus`, no `ILogger`, no `ILeakScanner`, no `IActorState<T>` — so each scoring boundary lands a single hit. Coverage added: empty-/whitespace-/punctuation-only query early-return; tag &gt; title &gt; description/narrative score-weight ordering; archived-filter at the state level; recency-boost edges pinned to the second (`Now-7d` boosts, `Now-7d-1s` does not; same shape at 30 days); future-dated `LastUsedAt`/`LastRecalledAt`/`OccurredAt` skips the boost (negative age path); `PreferRecent=false` and null `LastUsedAt` both skip the boost; `LastRecalledAt` takes precedence over `OccurredAt` as the recency anchor; `MinSuccessRate` clamps above 1 (filters everything below 1.0) and below 0 (lets everything through); `SuccessRate` multiplies the score 1:1 (4× difference in success rate produces 4× score gap); `RecallCount` boost shape verified to match `Math.Log2(count + 1)` to `1e-9`; agent-name and tag filters confirmed case-insensitive; `Since` filter inclusive at the cutoff; `MaxResults` truncates after ranking. No production code touched — pure test addition. Build clean (0 warnings, 0 errors); full suite 1912 → 1942 passed (+30) + 9 Docker-skipped.
- **2026-05-05** (`claude/next-handoff-job-a5xsa`) — Models-namespace consistency pass: the 97 data-class files that still declared `Weave.{Workspaces,Tools,Agents}.Models` now declare `Weave.X.<Folder>` matching the folder they live in (`Weave.Workspaces.{Lifecycle,Manifest,Registry,Templates}`, `Weave.Tools.{Connectors,Marketplace,Tool}`, `Weave.Agents.{Channels,Chat,Lifecycle,Memory,Skills,ToolRegistry,Users,Verification}`). 270 consumer files updated: `using Weave.X.Models;` collapsed into the precise set of folder usings based on which types each consumer references; fully-qualified `Weave.X.Models.TypeName` refs rewritten in place. Files that previously sat in the flat `Models` namespace now use explicit cross-folder usings within the same domain. Self-namespace usings stripped (76 files); duplicate usings deduped (82 files). Pure organizational refactor: tests stable at 1921 (1912 + 9 Docker-skipped) — same numbers as the previous session. The Wave 5 vertical-slice goal is now fully realized: every namespace under `Weave.{Workspaces,Tools,Agents}` matches its folder, with no horizontal-cut `Models/Services/Helpers/Utils/...` namespaces remaining in the three domain projects.
- **2026-05-05** (`claude/stabilization-work-xM6Z0`) — marketplace install scaffolds a workspace. New shared primitive `WorkspaceManifestFromTemplate.Create(template, workspaceName, isolation, agentName = "assistant")` ([WorkspaceManifestFromTemplate.cs](../src/Workspaces/Weave.Workspaces/Templates/WorkspaceManifestFromTemplate.cs)) produces a single-agent `WorkspaceManifest` from a `CapabilityTemplate` — the agent inherits the template's model/tools/capabilities, gets `SystemPromptFile = "./prompts/{agentName}.md"`, and the manifest carries `template.RequiredTools`, a `weave-{workspaceName}` network, env-secrets, and a default `local`/`podman` target. `MarketplaceInstallCliCommand` now prompts for a workspace name (default = template name) after a successful install API call, refuses to scaffold over a non-empty directory, and writes `workspace.json` + a `./prompts/assistant.md` derived from the template description. `WorkspaceNewTemplateFactory` was refactored so every preset path uses the primitive as the base: single-agent presets bottom out at it; `multi-agent` calls it with `agentName = "supervisor"` and layers a `worker`; `support-team` calls it with `agentName = "support-bot"` and layers a `monitor`. `TemplateResponse` (silo) and `ApiMarketplaceInstallTemplate` (CLI) both gained `AgentDefinition` + `Dictionary<string, ToolDefinition> RequiredTools` so the install response carries the full template config. Tests 1895 → 1912 (+17 focused unit tests on the primitive). The `WorkspacePresetCapabilitiesTests` suite was unchanged — the refactor is a behaviour-preserving move: every preset still emits the same agent keys, model, tools, and capabilities as before.
- **2026-05-04** (`claude/stabilize-code-quality-e3IFD`, [PR #58](https://github.com/KoalaFacts/Weave/pull/58)) — code-quality stabilization. Three new principles in [`docs/best-practices.md`](best-practices.md): (a) **Split, merge, refactor** — observable evidence-based tests for each action (different change rates / different consumers / replaceability / testability for split; co-change + single-caller + no-test-gain for merge; N≥3 duplication or named upcoming change for refactor) plus anti-principles (line count alone, "feels like two concerns" without git evidence, speculative future flexibility, "while I'm here" cleanups). (b) **`*Helpers` / `*Utilities` / `*Common` / `*Manager` are anti-pattern**, with the same suspicion on disguised `*Scorer` / `*Calculator` / `*Processor` static classes called from one or two places — narrow exception is verb-shaped pure-function modules with ≥3 distinct domain consumers. (c) **Return types and properties default to the immutable form** (`IReadOnlyList<T>` over `List<T>`, `init` over `set`); narrow exceptions for private fields/locals and Orleans-state record properties mutated in place before `WriteStateAsync`. Applied: deleted `SkillSearchScorer.cs` and `EpisodeSearchScorer.cs` (helper-class anti-pattern); search/recall behavior moved onto `SkillMemoryState.Search(...)` and `EpisodicMemoryState.Recall(...)` (Tell-don't-ask) with `private static Tokenize` per state record (no cross-domain reach). The `Weave.Agents.Memory → Weave.Agents.Skills` backward dependency is gone. State methods return `IReadOnlyList<T>`. Smaller fixes: `CommandDispatcher.cs` 13-line `<summary>` split into `<summary>` + `<remarks>`; narrating comment dropped from `GitHubActionsPublisher`; stale `using Weave.Agents.Skills;` removed from `EpisodicMemoryActor`. Net diff −10 lines. Tests stable at 1895 — refactor was a no-op for runtime behavior; testability gain is unrealized (state-method tests queued in Hygiene queue).
- **2026-05-04** (`claude/continue-handoff-tsEMD`) — Wave 5 vertical-slice splits, all three domains: `Weave.Workspaces` `Actors/Commands/Queries/Events/` → `Lifecycle/`, `Registry/`, `Templates/` (21 file moves, 24 consumers); `Weave.Tools` `Actors/Events/` → `Tool/`, `Marketplace/` (19 file moves, 19 consumers); `Weave.Agents` `Actors/Commands/Queries/Events/` → `Lifecycle/`, `ToolRegistry/`, `Channels/`, `Memory/`, `Skills/`, `Users/`, `Verification/` (62 file moves, 91 consumers — the biggest of the three). Behaviour namespaces (`Weave.X.Actors` etc.) renamed to feature-matching forms; data-class namespaces (`Weave.X.Models`) left alone for a separate consistency pass. Tests stable at 1904 across all three commits — pure organizational refactor, no behaviour change.
- **2026-05-04** (`claude/continue-handoff-tsEMD`) — `marketplace:install` shipped, vocabulary 6/6 → 7/7: `MarketplaceItem` gains an optional `TemplateId? TemplateId` linking the item to a published template. `IMarketplaceActor.InstallAsync(itemId, token)` is the install action — authorizes `marketplace:install` via the shared `CapabilityAuthorizer` (workspace-scope `null`, same as `plugin:invoke`), refuses non-Published items and items without a linked template (both 409), resolves the linked `CapabilityTemplate` via the global template actor, increments both install and instantiation counters, and returns a `MarketplaceInstallResult`. New `MarketplaceTokenFactory.MintInstall` mirrors the plugin token factory at the API boundary; new endpoint `POST /api/marketplace/{itemId}/install` returns `MarketplaceInstallResponse` (item + template). CLI `weave marketplace install [item-id]` follows the existing thin-API-wrapper pattern with guided + advanced modes. Tests 1895 → 1904 (+9: 5 new actor unit tests covering authorize / not-published / no-template / not-found / template-missing branches, 4 new integration tests covering happy path + draft-409 + 404 + no-template-409). Workspace scaffolding from the install response is queued as next work.
- **2026-05-04** (`claude/continue-handoff-tsEMD`) — preset/template duplication collapsed: `BuiltInTemplates` now exposes each template as a named `CapabilityTemplate` field (`Starter`, `CodingAssistant`, `Research`, `MultiAgentSupervisor`, `SupportBot`); `BuiltInTemplates.All` aggregates them. `PresetDefinition` (in `Weave.Cli`) is now a thin wrapper around a `CapabilityTemplate PrimaryTemplate` field, exposing `Model`, `Tools`, `ToolDefinitions`, and `Capabilities` as computed properties that delegate to the template's `AgentDefinition` and `RequiredTools`. `WorkspacePresets.All` constructs each entry by passing `BuiltInTemplates.X` directly, so the same object is referenced from both surfaces — edits land in one place. The 5-row drift-detector theory is gone, replaced by `EveryPreset_PrimaryTemplate_IsTheBuiltInTemplate` which makes the same guarantee via `ShouldBeSameAs`. `WorkspaceNewSelectionPrompt` and `WorkspaceNewTemplateFactory` had `?? []` and `?.ToolDefinitions is not null` defenses that became dead code (the wrapped properties are non-nullable); cleaned up. Tests 1899 → 1895 (−4: removed 5 InlineData rows from the old drift detector, added 1 reference-equality test).
- **2026-05-04** (`claude/continue-handoff-tsEMD`) — curated starter set + silo seeding: five `CapabilityTemplate` instances now ship in-repo as `BuiltInTemplates.All` (`Weave.Workspaces/Templates/BuiltInTemplates.cs`), one per primary agent shape (`starter`, `coding-assistant`, `research`, `multi-agent-supervisor`, `support-bot`). `BuiltInTemplateSeeder` (`Weave.Silo/Templates/BuiltInTemplateSeeder.cs`) is an `IHostedService` registered next to the audit subscriber that, on `StartAsync`, queries each `TemplateId` via the global `ICapabilityTemplateActor` and registers + publishes any missing entry. Idempotent — second runs and persistent backends skip every entry; validation failures log at `Error` and continue past the offending template rather than faulting startup. The CLI's `WorkspacePresets` retain the multi-agent and support-team composition data (worker, monitor, slack channel) — collapsing the preset/template duplication is the next session's work. A drift detector at `WorkspacePresetCapabilitiesTests.EveryPreset_PrimaryAgentShape_MatchesItsBuiltInTemplate` ratchets `Model`, `Tools`, and `Capabilities` together for all five preset/template pairs. Tests 1879 → 1899 (+20: 12 `BuiltInTemplatesTests` rows, 3 `BuiltInTemplateSeederTests`, 5 drift-detector theory rows).
- **2026-05-04** (`claude/continue-handoff-tsEMD`) — capability templates, lean-in pass: every workspace preset (`starter`, `coding-assistant`, `research`, `multi-agent`, `support-team`) now declares a coherent `Capabilities` array that flows through `WorkspaceNewSelection` → `WorkspaceNewTemplateFactory` → `AgentDefinition.Capabilities` in the emitted manifest, so `weave workspace new --preset coding-assistant` now produces a manifest where every tool the agent gets is matched by a `tool:<name>` grant. Custom flow (no preset) derives the same baseline from the chosen tool list. `CapabilityTemplateActor.Validate` tightened to emit `ToolCapabilityGranted:<tool>` per referenced tool plus `RequiredCapabilityGranted:<grant>` per `RequiredCapabilities` entry, both via segment-wise wildcard match (so `tool:*` covers any tool); null `AgentDefinition.Capabilities` coalesces to `[]` so the validator fails closed instead of throwing on an STJ/Orleans round-trip that drops the init-only initializer. The matcher itself moved down: the static `CapabilityToken.HasGrant(IEnumerable<string>, string)` + private `Matches` helper became `CapabilityGrantMatcher.HasGrant` in `Weave.Shared/Capabilities/`, so `Weave.Workspaces` (the validator) can call it without depending on `Weave.Security`. The instance `CapabilityToken.HasGrant(string)` stays on the token as a one-liner delegate; four `Weave.Agents` call sites updated, six `Weave.Security.Tests` static-method cases moved to `Weave.Shared.Tests/CapabilityGrantMatcherTests.cs`. Tests 1866 → 1879.
- **2026-05-04** (`claude/continue-handoff-work-U1Z7b`) — `secret:*` self-mint dropped: `ToolSecretResolver` now walks the tool definition first, collects the exact secret paths referenced in `Mcp.Env` values and `OpenApi.Auth.Token`, dedupes, and mints `[secret:p1, secret:p2, ...]` instead of `[secret:*]`. A definition with no secret refs skips the mint entirely. `ToolRegistryConnector.ConnectAsync` no longer adds `secret:*` to its mint — the token only needs `tool:{toolName}` (no concrete `IToolConnector` ever authorized against the secret grant; secret resolution happens before the connect token is minted, so the wildcard was dead grant). Tests: 4 focused `ToolSecretResolverTests` + 1 `ToolRegistryActorTests` end-to-end mint assertion verifying the connect token only carries `tool:{name}`. Tests 1861 → 1866.
- **2026-05-04** (`claude/continue-handoff-work-U1Z7b`) — signing-key rotation: `CapabilityTokenOptions.PreviousSigningKey` is a verify-only second key. `CapabilityTokenService.Validate` tries the current `SigningKey` first; on mismatch and only when `PreviousSigningKey` is configured, retries the same payload under the previous key. `Mint` always signs with the current key — the previous-key path is reachable only by tokens minted before the swap. Both keys carry the same minimum-length validation; whitespace `PreviousSigningKey` is treated as absent. `ComputeSignature` is now a static `(token, key) → base64` helper; the constructor stores `_previousSigningKey: byte[]?` only when configured. New tests: pre-rotation token validates post-rotation, post-rotation token only validates under current key, no-previous-key-configured rejects mismatched signatures, tampered grants still rejected even with rotation, short `PreviousSigningKey` throws, whitespace `PreviousSigningKey` falls through to "no previous key." Tests 1855 → 1861.
- **2026-05-04** (`claude/continue-handoff-work-U1Z7b`) — audit-row durability: `CapabilityAuditSubscriberHostedService` now wraps `store.Record` in a bounded retry loop (`MaxAttempts = 3`, exponential backoff with full jitter, `BaseDelay = 50ms`, `MaxDelay = 500ms`) using the injected `TimeProvider` so transient SQLite/Postgres write failures no longer drop the row. The subscriber's `_stopping` CTS short-circuits in-flight retries on `StopAsync`. New static `Counter<long>` `weave.silo.audit.write_failures` (meter `Weave.Silo.Audit`, registered in `Weave.ServiceDefaults.Extensions.ConfigureOpenTelemetry`) tags every failure with `outcome` (`"retried"` or `"dropped"`); pair its `dropped`-rate with `weave.security.capability.authorizations{outcome="deny"}` to alert when audit writes drop without denies dropping. New tests: `ThrowingStore` stub that fails N times then succeeds — covers eventual persistence + retried-counter, exhaustion-drops + dropped-counter, and the no-failure-no-metric case. Tests 1852 → 1855.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — CLI DI: introduced `Microsoft.Extensions.DependencyInjection` to `Weave.Cli` and a `CliServiceCollection.Build()` that registers the surfaces previously shaped as static-class-with-deps. `VersionService` is now an instance class (`TimeProvider` via ctor; pure helpers like `Current`, `IsNewer`, `LoadCache` stay static). `ChatExitConfirmation`, `ChatComposerEditor`, `ChatComposer`, `TuiShell`, `UpgradeCliCommand`, and `TuiCliCommand` are registered and resolved through the provider. `Program.cs` builds the provider once and passes it to `TuiApp.RunAsync` and to the `*Command.Create(handler)` factories at the composition root. Other CLI commands keep their existing static-factory pattern and migrate in here as they pick up real dependencies. Tests still 1852.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — hygiene wave 4: split `TuiSlashCommandDispatcher.cs` from 422 → 185 lines by extracting six per-responsibility view helpers (`TuiHelpView`, `TuiToolsView`, `TuiTasksView`, `TuiConfigView`, `TuiSystemView`, `TuiNewWorkspaceHint`). Dispatcher keeps the slash-command switch + two dispatcher-specific helpers. Each new file is ≤ 80 lines.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — hygiene wave 3: renamed `Weave.Dashboard/Services/` → `Weave.Dashboard/Api/`. The folder was a pluralized type-name smell; contents are exclusively API-contract types (`WeaveApiClient` + 8 DTOs + `DashboardJsonContext`). `git mv` preserves history; namespace updated; 6 page files updated their `using`.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — hygiene wave 2: `TimeProvider` threaded into the three behaviour-deciding sites that called `DateTime.UtcNow` directly. `AgentState` mutators (`SubmitTask`/`FailTask`/`SetAwaitingReview`/`AcceptTask`/`RejectTask`) take a `DateTimeOffset now` parameter; `AgentActor` passes `timeProvider.GetUtcNow()`. `ChatExitConfirmation` takes `TimeProvider` via ctor. `VersionService.KickOffRefreshIfStale`/`CheckAsync` take `TimeProvider` (later promoted to ctor injection in the CLI DI commit). 30+ test call sites updated mechanically.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — hygiene wave 1: `ChatComposer.TryCaptureCtrlC` catch syntax simplified from `catch (Exception ex) when (ex is X or Y) { /* ignore */ }` to two distinct `catch (IOException)` / `catch (PlatformNotSupportedException)` clauses, dropping the unused bind variable. Channel adapter payloads (Teams/Discord/Slack/Telegram/Email) replaced the AOT-unsafe `JsonSerializer.SerializeToUtf8Bytes(new { ... })` pattern with typed records and a new `ChannelPayloadJsonContext` (snake_case naming policy) so the source-gen pipeline picks them up.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — rule codification + dead-code removal: shipped a `check-rules` skill at `.claude/skills/check-rules/SKILL.md` that walks 16 audit categories with concrete `grep`/`find` commands and a `BLOCK`/`WARN`/`NOTE` punch-list output. Proactively invoked before claiming any code task done and on PR review. Codified rules in `docs/best-practices.md` for: pre-1.0 no back-compat shims (drop `Legacy*` constants, dual config keys, deprecated synonyms — already actioned in this branch); refactor-as-no-op discipline; force-regen lockfiles after graph changes; vertical slices with named violators; file/class size thresholds; `TimeProvider` injection; catch-clause hygiene; static-helpers-vs-DI; comment discipline; source-gen over reflection; dead-code attention; meaningful tests over verify-nothing. Acted on the source-gen rule in the same commit by deleting the dead `ServiceCollectionExtensions.AddCqrs` (`[RequiresUnreferencedCode]`, zero production callers, only its own tests referenced it). Tests 1860 → 1852.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — `"postgres"` alias dropped: canonical is `"postgresql"`. Removed the `PostgresProvider`/`PostgresBackend` constants and 5 consumer switches that paired the alias with the canonical (`SiloBuilderConfigurator`, `SiloServiceRegistrar`, `CliSecretResolver`, `SiloProcessService`, `WorkspaceSiloStarter`). Tests 1861 → 1860 (removed redundant `[InlineData("postgres", ...)]` row).
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — `Legacy*` config-key fallbacks dropped: `ActorStorageSettings`, `ContainerRuntimeSettings`, and `RuntimeSettings` no longer accept `Weave:Storage`/`Weave:StorageSchema`/`Weave:StorageDatabase`/`Weave:GrainStorage`/`Weave:Runtime:ContainerEngine`/`Orleans:ClusterId` shims. `SiloProcessService` (the only emitter) updated to use the modern `Weave:ActorStorage:{Provider,Schema,Database}` shape. Tests still 1861.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — Orleans clustering/persistence split: each backend now ships in its own project — `Weave.Silo.Clustering.Redis`, `Weave.Silo.Clustering.Sqlite`, `Weave.Silo.Clustering.SqlServer`, `Weave.Silo.Clustering.Postgres`. Each exposes a single `siloBuilder.AddXxxActorStorage(connectionString)` extension. `Weave.Silo.csproj` no longer directly references `Microsoft.Orleans.Persistence.{Redis,AdoNet}`, `Microsoft.Orleans.Clustering.{Redis,AdoNet}`, `Microsoft.Data.SqlClient`, `Npgsql`, or `Microsoft.Data.Sqlite` — they flow in via the 4 backend project refs. The default Silo build still bundles all backends (the `ConfigureActorStorage` switch in `SiloBuilderConfigurator` covers all 4 + memory), but anyone wanting a slim host can fork Weave.Silo, drop a backend ProjectReference, remove the matching switch case, and ship without those deps. Tests still 1861.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — storage-provider split: extracted `SqliteCapabilityAuditStore` into `Weave.Security.Sqlite` and `PostgresCapabilityAuditStore` into `Weave.Security.Postgres`. `Weave.Security.csproj` no longer pulls `Microsoft.Data.Sqlite` or `Npgsql` — anything that only needs the abstractions (`ICapabilityAuditStore`, `ICapabilityAuthorizer`, `ICapabilityTokenService`) gets a clean dependency graph. The Silo references both new projects directly (no plugin discovery — backends are wired by `CapabilityAudit:Backend` config). Namespaces: `Weave.Security.Sqlite` and `Weave.Security.Postgres`; `ICapabilityAuditStore` and `CapabilityAuditOptions` stay in `Weave.Security.Audit`. Tests still 1861.
- **2026-05-03** (`claude/continue-handoff-work-r25U1`) — allow/deny metrics: `CapabilityAuthorizer` increments a `Counter<long>` named `weave.security.capability.authorizations` on every authorize call (allow or deny) tagged with `outcome` and, on deny, `reason` (one of `invalid-or-expired-token` / `workspace-mismatch` / `grant-missing`). Static `Meter` named `Weave.Security.Capability` is registered in `Weave.ServiceDefaults.Extensions.ConfigureOpenTelemetry` so the existing OTLP pipeline exports it. `MeterListener`-based unit tests verify both outcome paths and all three deny reasons. Tests 1857 → 1861.
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — Testcontainers integration coverage for `PostgresCapabilityAuditStore`: `PostgresContainerFixture` boots a real `postgres:16-alpine` per test class; mirrors the full SQLite test matrix against the Postgres backend (chronological order, FIFO eviction, `GetRecent` with limit, all-fields preservation, durability across store instances). The fixture probes Docker by attempting to start the container; self-skips with the captured Docker error when the daemon isn't reachable, so Docker-less local boxes and restricted CI runners see Pass + Skip rather than Fail. Tests 1848 → 1857 (1848 + 9 skip-on-no-docker)
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — Postgres-backed audit store: `PostgresCapabilityAuditStore` selected via `CapabilityAudit:Backend = "postgresql"` (or `"postgres"`); `ConnectionString` required; pooled `NpgsqlConnection` per operation; same trim-on-insert FIFO bound. Targets multi-silo deployments where every silo writes into one shared `capability_audit` table. Config-validation unit tests cover the bootstrap surface. Tests 1845 → 1848
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — durable audit store: `SqliteCapabilityAuditStore` selected via `CapabilityAudit:Backend` (`"memory"` default, `"sqlite"` opt-in); auto-applies schema, FIFO trim-on-insert preserves the existing `Capacity` bound, default file `~/.weave/audit.db`. Selector wired in `SiloServiceRegistrar.RegisterCapabilityAuditStore()`. Tests 1835 → 1845
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — capability audit dashboard view: `/audit` (recent rows) and `/audit/{tokenId}` (per-token replay) Blazor pages, fed by new `WeaveApiClient.GetRecentCapabilityAuditAsync` / `GetCapabilityAuditByTokenAsync` over the existing `/api/audit/capability` endpoints. Token cells deep-link to per-token replay. No backend changes; tests still 1835
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — manifest-side wildcard matching: added static `CapabilityToken.HasGrant(IEnumerable<string>, string)`; replaced the four `state.Definition.Capabilities.Contains(grant)` literal checks (`AgentChatPipeline.EnrichWithUserContextAsync`, `SkillMemoryPromptEnricher.EnrichAsync` / `RecordSuccessfulUsageAsync`, `AgentSkillSuggester.SuggestFromTaskAsync`) with the segment-wise wildcard match. A manifest declaring `skill:*` now grants `skill:read`/`skill:write` for the manifest gate; tests 1827 → 1835
- **2026-05-03** (`claude/implement-next-task-vSX6x`) — `secret:<path>` enforcement moved out of `VaultSecretProvider` / `InMemorySecretProvider` into the shared `CapabilityAuthorizer`; allow + deny rows now flow through the audit pipeline, closing the last *Coverage of action types* gap (5/6 → 6/6); tests 1820 → 1827
- **2026-05-03** (`claude/competitor-analysis-handoff-Qn9jh`) — capability replay/debugger shipped; in-memory `ICapabilityAuditStore` (capacity-bounded) fed by a subscriber hosted service; `GET /api/audit/capability/{tokenId}` and `?limit=N` over CQRS query handlers; `weave audit replay [tokenId]` CLI with guided + advanced modes; tests 1807 → 1820
- **2026-05-03** (`claude/competitor-analysis-handoff-Qn9jh`) — capability-bound audit log shipped; five `Authorize` copies consolidated into `ICapabilityAuthorizer`; allow + deny rows publish `CapabilityAuthorizationEvent` keyed by tokenId/grant/workspaceId/issuedTo/outcome/reason/actionContext; tests 1790 → 1807
- **2026-05-03** (`claude/continue-agent-strategy-yS2Z1`) — `channel:*`, `user:*`, `plugin:invoke:*` shipped; capability cancellation linkage; mid-segment wildcards; `IReadOnlyList<T>` manifest cleanup; four PR #40 follow-ups; plugin types moved Workspaces → Silo to enable the verb
- **2026-05-03** — `skill:read` / `skill:write` shipped (PR #40)
- **2026-05-03** — Strategy doc + best-practices audit (PR #39)
